# WS4 Dual-Runtime Guard Audit Verdict - Round 3

- **Verdict:** `AUDIT_PASS`
- **Slice:** `ws4-dual-runtime-guard`
- **Requested gate:** scoped CC-WS4-001 round-3 code-correctness re-audit for clean-machine WSL-absence detection; not charter Gate-4 advancement
- **CivicCast repository:** `https://github.com/scottconverse/civiccast`
- **Audited SHA:** `aaf3fe24feb2366a11d16ee1098e1a9a29e4f64c`
- **PR base / direct parent / merge base:** `0d042dd2f6bd3873608c4d257e4e9f1455c910ab` (`program/native-windows`)
- **Branch / PR:** `claude/ws4-wsl-detect-clean-machine`; no PR exists for this branch
- **Codex auditor thread ID:** `019f8bfc-1e43-7393-9445-b24c85ed00c0`
- **Audit date:** 2026-07-22 (America/Denver)
- **Auditor environment:** NVIDEABLACKWELL; Microsoft Windows 11 Pro 10.0.26200 x64; PowerShell 5.1.26100.8875; Python 3.12.13; pytest 9.0.3; ruff 0.15.12; mypy 2.0.0; Git 2.54.0.windows.1; uv 0.11.15
- **Execution posture:** `sandbox=danger-full-access`; `approval_policy=never`
- **Detached worktree:** `C:\Users\scott\Desktop\CODE\_audit-worktrees\civiccast-ws4-dual-runtime-guard-aaf3fe24-r3`
- **Worktree state:** exact detached checkout at the audited SHA; clean before and after all proofs
- **Activity relay:** unavailable because GitHub has no PR for the branch; under `AUDIT_PROTOCOL.md` section 5 this does not invalidate the audit
- **Prior WS4 verdict:** round 2 `AUDIT_PASS` at `b94616c30fd2be2c451ed95ea8093155359d08db`

## Executive result

`AUDIT_PASS` is granted for the scoped CC-WS4-001 round-3 code-correctness fix at exactly `aaf3fe24feb2366a11d16ee1098e1a9a29e4f64c`. The new discriminator does not reopen the round-2 fail-open: an untrustworthy `wsl.exe` inventory returns definite `False` only when the SCM-backed service signal is exactly `False`; a present service (`True`) or ambiguous service query (`None`) preserves unknown inventory (`None`). The authoritative exit-0 decode path still returns directly without touching the service seam, and the A2 transitive path maps the clean-box no-service case to readable-negative.

The pass is deliberately narrower than pristine-machine proof or charter Gate-4 advancement. No fresh Windows Sandbox slot, actual Linux host, PR, or exact-head GitHub Actions run was available in this audit lane. Those absent proof classes are recorded below and are not promoted from hermetic or developer-machine results.

## Severity rollup

- Blocker: 0
- Critical: 0
- Major: 0
- Minor: 2, non-blocking under the owner's 2026-07-17 severity calibration
- Nit: 0

## Claims checked

### 1. Nonzero `wsl.exe` plus service absent returns `False` - proved

`detect_wsl_install` routes every nonzero inventory exit through `_confirm_absence_via_service` at `civiccast/native/win_probes.py:581-590`. The discriminator at line 522 uses identity comparison (`probe() is False`), so only exact definite absence authorizes `False`.

The committed control `test_detect_wsl_install_nonzero_wsl_with_service_absent_is_definite_false` passed, and an auditor-authored independent inline control reproduced `False` with one service-seam call.

### 2. Nonzero `wsl.exe` plus service present returns `None` - proved

The committed present-service control passed. The independent control reproduced `None`, and a deliberately mutated implementation that collapsed every service result to `False` made both the direct control and the end-to-end CC-WS4-001 blocked-path control fail. This is the central fail-open falsification.

### 3. Nonzero `wsl.exe` plus ambiguous service query returns `None` - proved

The committed ambiguous-service control passed and the independent control reproduced `None`. `_default_wsl_service_present` returns `None` for unexpected exit codes, timeout, or `OSError` at `civiccast/native/win_probes.py:490-499`; `_confirm_absence_via_service` preserves that ambiguity.

### 4. Exit-0 inventory path is unchanged and never touches the service seam - proved

The final return at `civiccast/native/win_probes.py:587-590` remains `WSL_DISTRO_NAME in output` after clean decode. A raising sentinel recorded zero service-seam calls for both exit-0/no-distro (`False`) and exit-0/CivicCast-distro (`True`). The committed sentinel test and the independent control both passed.

The two protected real-fire test function bodies are byte-identical between base and head:

- `test_detect_wsl_install_false_on_this_distro_less_box`: SHA-256 `0d2e770fcb06b600220bd57e746120d1a66073c57acc88c3e13842fd42ab4581`
- `test_probe_indistro_services_real_fire_negative_no_distro_registered`: SHA-256 `df5a16e0a4d5f57bfc20a8d983f2e069afbce3a3afe4eeb1190c2d76fb50ca3e`

Both passed on this developer machine through its exit-0 `wsl.exe -l -q` path.

### 5. A2 knock-on maps systemctl failure plus service absence to negative - proved

`probe_indistro_services` routes timeout/invocation failure through `_classify_absence_via_detect` at `civiccast/native/win_probes.py:622-650`. That classifier returns A2 negative only for identity-exact `installed is False` at lines 608-618. The committed A2 knock-on control passed, and an independent transitive control reproduced `status="negative"` with detail `no CivicCast distro registered`. Feeding that result, selector-absent, install-False, and otherwise-clear inputs into `decide()` produced `action="start"`.

### 6. SCM classifier, platform-import boundary, and strict typing - proved within stated boundary

The SCM classifier queried the absolute `C:\Windows\System32\sc.exe` path, checked `WslService` and `LxssManager`, returned `True` on any exit 0, returned `False` only after both names returned 1060, and returned `None` on any other result. All seven committed classifier controls passed. On the real audit host, `sc.exe query WslService` returned 0 with the service running; `sc.exe query LxssManager` returned 1060. Microsoft documents 1060 as `ERROR_SERVICE_DOES_NOT_EXIST`, and its WSL FAQ documents the Store transition from `LxssManager` to `WslService`:

- <https://learn.microsoft.com/en-us/windows/win32/debug/system-error-codes--1000-1299->
- <https://learn.microsoft.com/en-us/windows/wsl/faq>

`python -m mypy --strict civiccast/native` succeeded with no issues in five source files. A fresh-process import with `winreg`, `win32event`, `win32security`, `win32api`, and `pywintypes` forcibly blocked succeeded for `civiccast.native.win_probes`; source inspection confirms those modules remain lazy imports. This is static plus simulated platform-module-absence proof, not an actual Linux runtime execution.

## Proofs re-run

- `git ls-remote origin refs/heads/claude/ws4-wsl-detect-clean-machine` resolved the remote branch to the audited SHA.
- `git rev-parse HEAD^`, `git merge-base 0d042dd2f6bd3873608c4d257e4e9f1455c910ab HEAD`, and `git rev-list --count 0d042dd2f6bd3873608c4d257e4e9f1455c910ab..HEAD` proved the supplied base is the direct parent and the diff contains one commit.
- GitHub's commit API reports the coder commit signature valid and verified.
- `git diff --name-status 0d042dd2f6bd3873608c4d257e4e9f1455c910ab..HEAD` contains only `civiccast/native/win_probes.py` and `tests/native/test_win_probes.py`; `git diff --check 0d042dd2f6bd3873608c4d257e4e9f1455c910ab..HEAD` returned no findings.
- `python -m pytest tests/native -q` -> **182 passed in 9.97s**.
- Targeted discriminator/SCM/A2/real-fire panel -> **14 passed, 61 deselected**.
- Independent verifier panel over the affected detector family -> **22 passed, 53 deselected**.
- `python -m ruff check civiccast/native tests/native` -> all checks passed.
- `python -m mypy --strict civiccast/native` -> success, no issues in five source files.
- Independent inline falsification -> nonzero+absent `False`; nonzero+present `None`; nonzero+ambiguous `None`; exit-0 service calls 0; A2 negative; final clear `decide()` start.
- Mutation control in a separate disposable detached worktree changed the discriminator to return `False` for present/ambiguous signals. The exact fail-open controls went red: **3 failed**, pytest exit 1. The mutation worktree was then removed; the audited worktree remained clean.
- Real developer-machine probes -> `wsl.exe -l -q` exit 0; `sc.exe query WslService` exit 0; `sc.exe query LxssManager` exit 1060; `_default_wsl_service_present()` returned `True`.

## Findings

### CC-WS4-010 - Minor - detector docstring retains superseded round-2 exclusivity claims

`civiccast/native/win_probes.py:534-559` still says failure paths return `None` and `False` is returned only after a successful trustworthy `wsl.exe` inventory. The later round-3 paragraph at lines 561-578 correctly states the new SCM discriminator, so the same docstring contradicts itself. `civiccast/native/models.py:164-169` likewise lists timeout, `OSError`, and undecodable output as always unknown without mentioning definite service absence.

This does not alter runtime behavior or the executable proof. Under the owner's severity calibration it is batched Minor wording drift. Smallest acceptance criterion: consolidate the docstrings so they state the round-3 rule once and do not preserve the superseded exclusivity statement.

### CC-WS4-011 - Minor - Windows CI collection floor remains 168 after the suite grew to 182

`.github/workflows/ci-test.yml:521-523` says the full-suite floor must be bumped whenever a native test is added, but line 556 still sets `floor = 168`; this commit adds 14 tests and `pytest --collect-only` reports 182. The workflow still executes the whole test directory and independently fails on every recorded skip/failure, so this stale lower bound did not invalidate the auditor's exact-SHA local execution. It weakens future detection of lost test collection.

Smallest acceptance criterion: update the Windows floor to 182 with the next appropriate source change, and keep the floor synchronized whenever native tests are added or intentionally removed.

## CI, clean-machine, and field-proof boundaries

- `gh pr list --head claude/ws4-wsl-detect-clean-machine --state all` returned no PR.
- `gh run list --branch claude/ws4-wsl-detect-clean-machine` returned no runs.
- GitHub check-runs for the audited SHA returned total count 0, and combined commit status had no statuses. The request's `CI: see branch checks` therefore had no available evidence to inspect.
- `.github/workflows/ci-test.yml` triggers only on pull requests targeting `main` or `program/**`; the no-PR branch did not trigger it.
- No Windows Sandbox slot was available in this audit lane. The coordinator's stated WSL-less clean-machine rerun remains pending and is not represented as passed.
- No actual Linux runtime was available: this Windows host has no registered WSL distro and no Docker executable. Linux importability is supported by static review, forced platform-module absence, and strict typing, not an actual Linux process.
- No CivicCast WSL co-install, systemd keeper, native supervisor, session-0 service, physical device, cutover, rollback, or soak was exercised.

Confidence achieved:

- **Class 1:** exact detached checkout, complete scoped diff/final-state review, caller/data-contract trace, commit/signature/base identity, Microsoft service-name/error-code source check, and protected-test byte comparison.
- **Class 2:** complete native suite, targeted controls, independent controls, mutation-tested fail-open verifier, lint, strict typing, and simulated platform-module-absence import in the detached worktree.
- **Class 3:** read-only developer-machine `wsl.exe` and SCM queries on NVIDEABLACKWELL.
- **Not achieved:** class 4 session-0, class 5 live peer/hardware, class 6 independently pristine Windows, or class 7 soak.

## Source-control and system actions

No CivicCast source implementation edit, coder-branch edit, merge, PR creation, tag, release, rc-line action, install, co-install, selector/interlock mutation, service mutation, cutover, or rollback was performed. The auditor created the required detached audit worktree and a separate temporary mutation worktree; only the temporary mutation worktree was removed after its expected-red result. Scott Converse remains owner and tie-breaker.

## Final rationale

The changed implementation is fail-closed at the precise boundary the round-3 fix introduces: only the conjunction of an untrustworthy inventory and a definite no-service signal becomes `False`; service-present and service-ambiguous states remain `None`. The authoritative exit-0 path bypasses the new seam, the A2 knock-on is correct, the relevant tests detect an intentionally reopened fail-open, and the full local suite, lint, and strict typing pass at the exact SHA.

`AUDIT_PASS` binds only to CivicCast SHA `aaf3fe24feb2366a11d16ee1098e1a9a29e4f64c` and only to this scoped code-correctness gate. It authorizes no merge by the auditor and no CivicCast `main` action, tag, release, co-install, Gate-4 advancement, or cutover. Any source change requires a new exact-SHA audit. Scott Converse remains the sole owner authority for integration decisions.
