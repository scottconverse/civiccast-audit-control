# WS2 Postgres Restore Re-audit Verdict - Round 6

- **Verdict:** `AUDIT_PASS`
- **Slice:** `ws2-postgres-restore`
- **Requested gate:** Charter program-sequence item 2 / Workstream 3 item 1, Postgres restore drill against the shipping WSL product
- **CivicCast repository:** `https://github.com/scottconverse/civiccast`
- **Audited SHA:** `38866d0f6b9f446b57c82b0ad2649b6546a75220`
- **Prior audited SHA:** `a6dd4f4e1cd0407e9d99fdfd202968390050068f` (`CHANGES_REQUIRED`, audit-control commit `f41fd33e`)
- **Branch / PR:** `claude/ws2-postgres-restore` / CivicCast PR #292, targeting `program/native-windows`
- **Current base ref SHA:** `3ca1d44d4b6497d6167b95f2607e8dd26c94806f`
- **Historical merge base:** `39d939429cdd1cb094538b2e2d8a76ac4fb05c60`
- **Codex thread ID:** `019f6cfb-27e1-7240-82ba-b81aaca03ca3`
- **Audit date:** 2026-07-17 (America/Denver)
- **Auditor environment:** Microsoft Windows 10.0.26200 x64; PowerShell 5.1; uv-managed Python 3.12.13; Git 2.54.0.windows.1
- **Execution posture:** `sandbox=danger-full-access`; `approval_policy=never` (headless / non-interactive)
- **Detached worktree:** `C:\Users\scott\Desktop\CODE\_audit-worktrees\civiccast-ws2-postgres-restore-38866d0f-r6`
- **Worktree state:** exact, detached, and clean before and after verification
- **Activity relay:** <https://github.com/scottconverse/civiccast/pull/292#issuecomment-5003532757>
- **Heartbeat result:** start and all applicable phase transitions were relayed; no relay failure occurred

## Executive result

Round 6 closes the sole blocking finding from the round-5 verdict. The commit adds three Docker-gated controls that each invoke the real `run_postgres_cold_standby_drill`, assert `report.ok is False`, and require the diagnostic for the exact drift under test: a standby-only membership edge touching a relevant role, a PUBLIC table grant difference, and an otherwise identical table privilege whose grantability differs. These are not helper-only equality tests.

The controls are non-vacuous. The membership test additionally requires the error to name the standby-only role; reverting to the old both-endpoint filter removes that error. The PUBLIC test requires a table-grant error containing `PUBLIC`; the predecessor information-schema collector cannot produce it. The grant-option test changes only `is_grantable`; the old three-field projection compares equal and cannot produce the required table-grant mismatch. The source-side mutation arrangement remains the accepted way to preserve ACL drift across the production function's unconditional drop/create/restore behavior.

CI run `29580769014` executed all three controls against PostgreSQL testcontainers, and the exact-file guard reported 20 passed, zero skipped, floor 20. The CI synthetic merge contains byte-identical implementation, test, workflow, and evidence blobs to the audited head. No functional finding remains for the requested gate.

One previously batched Minor documentation finding remains. Under the owner's severity calibration it does not block this verdict.

## Prior-finding closure

| Finding | Status at `38866d0f` | Assessment |
|---|---|---|
| CC-WS2-001, role/table-ACL false passes | **CLOSED** | The round-5 production fixes remain unchanged and now have the three required report-level controls. |
| CC-WS2-002, helpers not product | **CLOSED, no regression** | Shipping dispatch and real cold-standby report execution remain covered. |
| CC-WS2-003, manifest not snapshot-bound | **CLOSED, no regression** | Exported-snapshot binding and its ABA control remain in the executed inventory. |
| CC-WS2-005, incomplete JUnit floor | **CLOSED** | The committed inventory and workflow floor are both 20; CI observed 20 passed and zero skipped. |
| CC-WS2-006, missing report-level controls | **CLOSED** | All three exact acceptance controls are committed and executed through the real report. |
| CC-WS2-007, prose/encoding batch | **PARTIALLY CLOSED, non-blocking** | The three mojibake em dashes and BOM concern are closed. Stale PR/evidence/membership wording remains as Minor. |

## Severity rollup

- Blocker: 0
- Critical: 0
- Major: 0
- Minor: 1 batched documentation finding
- Nit: 0

## Non-blocking finding

### CC-WS2-007 - Minor - Review surfaces still describe an earlier round

- PR #292 still calls itself the round-4 state, reports 17 tests/floor 17, and ends its audit trail at round 4.
- The new evidence section is headed “Round 5” although this is round 6, says CI is at the “round-5 head,” and literally says `restore_drill.py` was not touched even though this commit restores three characters in that file. Its substantive statement that no logic changed is correct.
- Two existing docstrings around `_pg_all_role_membership_edges` and `_close_roles_over_memberships` still describe `_pg_role_memberships` as retaining only edges whose two endpoints are relevant; production now correctly uses either endpoint.
- **Disposition:** reconcile these surfaces before the relevant near-release documentation gate. They do not misstate runtime behavior in a way that changes an operator action, so the owner decision requires batching them as Minor rather than blocking this slice.

## Proofs re-run and evidence inspected

- Live PR #292 is open, non-draft, mergeable, targets `program/native-windows`, and has exact head `38866d0f6b9f446b57c82b0ad2649b6546a75220`. GitHub reports a valid SSH signature for the audited commit.
- The round-6 commit has parent `a6dd4f4e...`. Its diff changes the three test controls, CI floor/comment, evidence README, and three docstring characters. `restore_drill.py` has no logic change from the implementation accepted in round 5.
- `git diff --check a6dd4f4e..38866d0f` passed. The detached worktree remained clean.
- `uv run pytest tests/dr/test_postgres_restore.py --collect-only -q` -> exactly 20 tests collected, including all three named controls.
- `uv run pytest tests/dr/test_postgres_restore.py -q -rs` -> 2 passed, 18 Docker-gated skips locally.
- `uv run pytest tests/dr -q -rs` -> 23 passed, 19 Docker-gated skips locally.
- With `CIVICCAST_RUN_POSTGRES_TESTS=1` and Docker absent, the first required test hard-fails at setup instead of skipping.
- `uv run ruff check civiccast/dr/restore_drill.py tests/dr/test_postgres_restore.py` -> all checks passed.
- `uv run mypy civiccast/dr` -> success, no issues in six source files.
- Repo-wide `civiccast/**/*.py` UTF-8 BOM scan -> clean. `restore_drill.py` contains the three intended U+2014 em dashes and zero mojibake sequences.
- CI run `29580769014` is successful with event head SHA exact. Its logs enumerate the three new tests as passed and report `Postgres restore-drill tests passed: 20 (skipped: 0; floor: 20)`; overall run: 5653 passed, 30 skipped.
- GitHub Actions checked out synthetic merge `1f0dded1bcf2c0fc64fafb07812d18072164d6da`, with parents current base `3ca1d44d...` and audited head `38866d0f...`. Git blob IDs for `restore_drill.py`, `test_postgres_restore.py`, `ci-test.yml`, and the evidence README exactly match the audited head.

## Confidence and boundaries

- **Principal-engineering lens:** traced each mutation through production backup/replay/restore/catalog comparison and confirmed no runtime logic changed from the accepted implementation.
- **Test-engineering / QA lenses:** checked that each assertion depends on its target field, reran collection/local fail-closed/static checks, and attestation-verified real PostgreSQL execution through exact blob identity. Docker is unavailable locally, so CI is explicitly the PostgreSQL runtime proof.
- **Technical-writing lens:** found the remaining Minor review-surface drift above. UI/UX is not applicable to this database-only slice.
- This verdict is software-only and does not claim media, configuration, credentials, pristine-Windows, session-0, LPM, rollback, release, or cutover proof.
- No coder branch, CivicCast source, PR branch, merge, tag, release, or rc-line state was modified by the auditor.

## Governance and authorization

- Scott's `decisions/2026-07-17-severity-calibration.md` was applied: no functional defect remains; the prose batch is Minor and non-blocking.
- Scott's `decisions/2026-07-17-testing-policy.md` was applied: no hardware, LPM, or soak proof was demanded for this software-only database gate.
- This `AUDIT_PASS` is bound only to CivicCast SHA `38866d0f6b9f446b57c82b0ad2649b6546a75220`.
- Per the standing owner rule, it authorizes integration only into `program/native-windows`, never CivicCast `main`; it authorizes no tag, release, rc-line action, or cutover.
- The WS3 claims-evidence slice is separate and was not assessed in this verdict.

Scott Converse remains owner and tie-breaker. Nothing merges on the auditor's verdict alone.
