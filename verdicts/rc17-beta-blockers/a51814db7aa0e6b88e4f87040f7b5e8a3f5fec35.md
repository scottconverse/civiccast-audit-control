# rc17 Beta-Blocker Salvage Round-2 Audit Verdict

- **Verdict:** `AUDIT_PASS`
- **Slice:** `rc17-beta-blockers`
- **Requested gate:** round-2 closure of the exact-SHA rc17 salvage code audit; the exact-artifact pristine-machine gauntlet remains a later owner-held release gate
- **CivicCast repository:** `https://github.com/scottconverse/civiccast`
- **Audited SHA:** `a51814db7aa0e6b88e4f87040f7b5e8a3f5fec35`
- **Audited parent:** `236850b8fd20db418b0ec34593d90f78857c7680` (round-1 `CHANGES_REQUIRED`)
- **Base / merge base:** `0deac05e935740cfd55c32a737cbd23eff6ea91a` (rc16)
- **Branch / PR:** `fix/rc17-beta-blockers` / CivicCast PR #299, draft, targeting `main`
- **Codex thread ID:** `019f74cb-7d7d-7762-8246-f666f424c91d`
- **Audit date:** 2026-07-18 (America/Denver)
- **Auditor environment:** DESKTOP-VBMA6O5; Microsoft Windows 11 Pro 10.0.26200 x64; PowerShell 5.1.26100.8875; Python 3.12.10; pytest 9.0.3; Git 2.54.0.windows.1; uv 0.11.23; ruff 0.15.12
- **Execution posture:** `sandbox=danger-full-access`; `approval_policy=never`
- **Detached worktree:** `C:\Users\Scott\Desktop\CODE\_audit-worktrees\civiccast-rc17-beta-blockers-a51814db-r2`
- **Worktree state:** exact, detached, and clean at binding and verdict writing
- **Activity relay:** <https://github.com/scottconverse/civiccast/pull/299#issuecomment-5013358179>
- **Owner scope:** option 1 under `decisions/2026-07-18-rc17-audit-scope.md`; this pass grants no `main` merge or tag authority

## Executive result

Round 2 closes both round-1 Major findings and the batched documentation items.

Provider readiness now gives every ready-capable real provider's production settings loader first and unconditional authority. Complete Setup-stored field names cannot bypass Internet Archive, YouTube, local NAS, or SMTP validation. Subscriber notifications is checked through the same `SmtpSettings.from_env()` method used by the mapped production mail adapter. The four literal round-1 falsifications are green at this SHA and all four fail for the expected false-ready behavior when run against the exact parent production module.

All six live current-release surfaces now label Ollama provisioning and UAC re-elevation as forthcoming rc17-candidate behavior absent from published rc15. The genuinely rc15 helper re-probe sentence remains outside that label and matches the rc16 base. The new policy check reports zero findings at the audited head and 12 location-bearing violations when executed against the exact round-1 parent documents.

No blocking functional defect was found in the round-2 delta. The code audit passes. This does not satisfy or waive the charter's exact-artifact pristine-Windows install, real inference, rehearsal/playback, returning-Setup/UAC, or cold-reboot gauntlet.

## Prior-finding reconciliation

| Finding | Status at `a51814db` | Result |
| --- | --- | --- |
| CC-RC17-001 | Closed | Real settings-loader validation runs before stored-field fallback for every ready-capable mapped real provider; all four literal falsifications now fail closed. |
| CC-RC17-002 | Closed | Six live surfaces separate current rc15 fact from forthcoming rc17 behavior; the real-parent boundary scan produces 12 violations and the final tree produces zero. |
| CC-RC17-003 | Closed | The three named docs now describe a common three-tag target with missing-only downloads. |
| Round-1 batched nit | Closed | The duplicated "roughly" was removed and the manual artifacts were regenerated. |

## Severity rollup

- Blocker: 0
- Critical: 0
- Major: 0
- Minor: 0
- Nit: 0

## Findings

No new finding.

## Proofs re-run and evidence inspected

- Live PR #299 was open and deliberately draft, targeted `main`, and named exact head `a51814db7aa0e6b88e4f87040f7b5e8a3f5fec35`. Its parent is exactly the round-1 audited SHA, and the rc16 source remains the exact merge base. The source commit reported a valid Git signature.
- The complete one-commit, 16-file round-2 delta and final adjacent state were reviewed. The delta contains 704 insertions and 66 deletions. `git diff --check` passed and the final worktree remained clean.
- At `civiccast/installer/service.py:429`, the real-validation set now includes Internet Archive, YouTube, local NAS, and subscriber notifications. `civiccast/installer/service.py:434` routes those providers to the same settings classes their production adapters use, including `SmtpSettings.from_env()` at `civiccast/installer/service.py:457`. `_provider_credentials_are_valid()` checks that real-loader set first at `civiccast/installer/service.py:491`; stored field-name fallback follows only for providers without a real loader at `civiccast/installer/service.py:493`.
- The four new service controls at `tests/installer/test_provider_readiness_fail_honest.py:152` through `tests/installer/test_provider_readiness_fail_honest.py:266` cover complete stored Internet Archive credentials with absent environment, complete stored YouTube credentials with absent environment, YouTube without refresh token, and subscriber webhook-only configuration. They verify both non-ready status and proof rejection.
- Head focused execution of the nine provider-readiness tests plus nine release-boundary tests returned 18 passed in 4.13 seconds. The direct final-tree boundary command also passed.
- The exact four new D4 tests were executed with the exact parent worktree forced onto Python's import path. All four failed at the old `needs_live_proof` false-positive state, returning 4 failed and 5 deselected. This proves the controls discriminate the round-1 defect rather than merely exercising the new code.
- The release-boundary analyzer is registered in `scripts/policy/run_all.py:39`. Its curated six surfaces and candidate markers are defined at `scripts/policy/check_release_candidate_boundary.py:52` and `scripts/policy/check_release_candidate_boundary.py:66`; block-local qualification is enforced at `scripts/policy/check_release_candidate_boundary.py:125`.
- Running the head analyzer against the exact parent documents produced 12 findings: three in `INSTALL-WINDOWS.md`, three in `FAQ.md`, two in `docs/tester/START-HERE.md`, and one each in the LPM handoff, known limitations, and two USER-MANUAL blocks. Running it against the final tree produced zero.
- The round-2 doc diff was also compared directly with rc16. The valid rc15 helper re-probe behavior remains plain current-release text at `INSTALL-WINDOWS.md:99`; only the new re-elevation behavior begins under the forthcoming label at `INSTALL-WINDOWS.md:105`.
- The current docs consistently say the target set has three tags and only missing tags are downloaded, including `INSTALL-WINDOWS.md:63`, `docs/USER-MANUAL.md:257`, and `docs/installer/beta-tester-handoff.md:33`.
- Full local `tests/policy tests/installer` execution returned 571 passed and three environmental skips in 155.34 seconds. The skips were the unchanged Linux-only `systemd-analyze` check and two explicitly network-gated TSDuck release-pin checks.
- Repository-wide ruff check passed; ruff format check reported 1,170 files already formatted. The tracked PDF/DOCX manual render check passed. No BOM was present in any changed file, and diff hygiene was clean.
- The aggregate policy runner executed the new boundary check successfully. It also reported two unrelated whole-repo baseline failures in public-copy legal scanning and FFmpeg-wrapper scanning. Both checks return the same failure on exact parent `236850b8`, neither round-2 changed path contributes a reported violation, and neither is attributed to this delta.
- The supplied Actions URL `29664804833` is the exact-head one-job `roadmap-status` workflow. The live PR check suite provides the claimed complete evidence: 17 successful exact-head jobs across unit/GStreamer/NATS, Docker cleanroom, installer Rust/frontend, lint, docs, accessibility, security, operator build, and roadmap workflows.

## Five-dimension result

- **Correctness and security:** the false-ready short circuit is removed; real adapter loaders control readiness and proof admission. No new secret exposure or data-path change was introduced.
- **UX:** readiness now remains honest and surfaces the real loader's configuration error. No visual component changed in round 2.
- **Documentation:** current rc15 instructions and forthcoming rc17 behavior are separated on every scoped live surface; the manual binaries match their source.
- **Tests:** the four literal D4 cases and real-parent documentation reconstruction are effective expected-red controls; focused and broad suites pass.
- **Runtime:** service-level provider transitions and the policy command were executed locally. Installed-artifact Windows behavior remains deliberately outside this code-audit confidence layer.

## Confidence and boundaries

- **Class 1:** exact detached checkout, complete parent delta/final-state review, rc16 comparison, charter/governance reconciliation, and live PR/check inspection.
- **Class 2:** head and exact-parent falsification execution, focused and broad test suites, direct policy execution, ruff, formatting, render freshness, BOM, and diff hygiene.
- **Class 3 / hosted CI:** 17 exact-head PR checks, including Docker cleanroom and Windows installer compile/test jobs. Hosted CI is not an exact-artifact pristine Windows installation.
- **Not established:** real Ollama installation/service startup, real model downloads, real caption/summary/translation inference, exact-artifact rehearsal-to-playback, actual returning-Setup/UAC interaction, or cold-reboot recovery. Those remain the scheduled class-6 gauntlet.
- The boundary policy is intentionally curated and lexical. Future capability wording or a new live release surface must be added explicitly; current six-surface behavior and the exact parent regression are covered.
- The four CDN providers remain on their existing field/env-name validation path because no mapped real `.from_env()` adapter exists and they cannot reach `ready`; this round did not change that architecture.
- The separate `run_first_health_check()` legacy credential-name surface remains outside this finding and unchanged, as scoped in round 1.
- No CivicCast source edit, coder-branch edit, merge, tag, release, clean-machine installation, LPM action, or public-release change was performed.

## Governance and final rationale

The exact-parent controls prove that round 2 closes the false-ready paths and the release-identity documentation mixing identified in round 1. The final source, tests, live docs, rendered artifacts, broad regressions, and exact-head hosted checks agree. No functional finding remains in this round-2 scope.

`AUDIT_PASS` binds only to CivicCast SHA `a51814db7aa0e6b88e4f87040f7b5e8a3f5fec35`. It permits this exact candidate to proceed to the owner-scheduled pristine-machine gauntlet; it does not authorize a merge to CivicCast `main`, a tag, a release, or an LPM handoff. Scott Converse remains owner and tie-breaker.
