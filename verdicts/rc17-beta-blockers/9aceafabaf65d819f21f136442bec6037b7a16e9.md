# rc17 Beta-Blocker Salvage Round-3 Audit Verdict

- **Verdict:** `AUDIT_PASS`
- **Slice:** `rc17-beta-blockers`
- **Requested gate:** round-3 review of the clean-Windows proof-verifier fix; rebuilt-artifact pristine-machine gauntlet remains a later owner-held release gate
- **CivicCast repository:** `https://github.com/scottconverse/civiccast`
- **Audited SHA:** `9aceafabaf65d819f21f136442bec6037b7a16e9`
- **Audited parent:** `a51814db7aa0e6b88e4f87040f7b5e8a3f5fec35` (round-2 `AUDIT_PASS`)
- **Base / merge base:** `0deac05e935740cfd55c32a737cbd23eff6ea91a` (rc16)
- **Branch / PR:** `fix/rc17-beta-blockers` / CivicCast PR #299, draft, targeting `main`
- **Codex thread ID:** `019f74cb-7d7d-7762-8246-f666f424c91d`
- **Audit date:** 2026-07-19 (America/Denver)
- **Auditor environment:** DESKTOP-VBMA6O5; Microsoft Windows 11 Pro 10.0.26200 x64; Windows PowerShell 5.1.26100.8875; Python 3.12.10; pytest 9.1.1; Git 2.54.0.windows.1; uv 0.11.23; ruff 0.15.18
- **Execution posture:** `sandbox=danger-full-access`; `approval_policy=never`
- **Detached worktree:** `C:\Users\Scott\Desktop\CODE\_audit-worktrees\civiccast-rc17-beta-blockers-9aceafab-r3`
- **Worktree state:** exact, detached, and clean at binding and verdict writing
- **Activity relay:** <https://github.com/scottconverse/civiccast/pull/299#issuecomment-5014792216>
- **Owner scope:** option 1 under `decisions/2026-07-18-rc17-audit-scope.md`; this pass grants no `main` merge or tag authority

## Executive result

The round-3 change fixes the proof verifier's clean-machine WSL enumeration failure. The generated PowerShell now sends `wsl.exe` stderr through `cmd.exe` redirection before PowerShell can promote native stderr under its stop-on-error policy, and it initializes the result to an empty distribution list. The exact new control passes at the audited head and fails against the exact round-2 parent generator.

An auditor-executed Windows PowerShell probe used a PATH-isolated fake `wsl.exe` that emitted the reported no-WSL stderr and exited 1. The former direct invocation terminated and entered `catch`; the new exact command continued with exit 1 available and zero distributions. The complete package-artifact test module returned 47 passed, and the generated verifier parsed with zero PowerShell AST errors.

No blocking functional defect was found in this release-tooling-only delta. The source review passes. The currently banked artifact contains the pre-fix verifier and is not valid gauntlet input. A fresh artifact built from this reviewed source is mandatory, and its full clean-Windows install, inference, and reboot gauntlet remains unexecuted and unproved by this verdict.

## Severity rollup

- Blocker: 0
- Critical: 0
- Major: 0
- Minor: 0
- Nit: 0

## Findings

No new finding.

## Proofs re-run and evidence inspected

- Live PR #299 was open and deliberately draft, targeted `main`, and named exact head `9aceafabaf65d819f21f136442bec6037b7a16e9`. Its parent is exactly the round-2 audited SHA, and the rc16 source remains the branch merge base.
- The complete one-commit delta was reviewed. It changes only `scripts/build_release_artifacts.py` and `tests/installer/test_package_artifacts.py`, with 39 insertions and one deletion. No D1-D6 product/runtime surface changed.
- The generated verifier documents and implements the clean-machine failure handling at `scripts/build_release_artifacts.py:923`. It initializes the distribution result at `scripts/build_release_artifacts.py:929`, invokes `cmd.exe` with stderr redirected to `nul` at `scripts/build_release_artifacts.py:931`, normalizes successful output at `scripts/build_release_artifacts.py:932`, and defaults to an empty list on a thrown failure at `scripts/build_release_artifacts.py:933`.
- The committed regression control begins at `tests/installer/test_package_artifacts.py:1390`. It rejects the old direct invocation at `tests/installer/test_package_artifacts.py:1409` and requires both the cmd-level redirect and empty-list handling at `tests/installer/test_package_artifacts.py:1412`.
- The exact head control returned 1 passed. Running that same head test against the exact parent production generator returned the expected assertion failure because the parent still emitted the direct `& wsl.exe --list --quiet 2>$null` form.
- A Windows PowerShell 5.1 runtime falsification compiled a temporary PATH-isolated `wsl.exe` that wrote `WSL is not installed` to native stderr and exited 1. The old exact direct command produced a terminating error; the new exact cmd-redirect command continued, reported native exit 1, and produced a zero-item distribution list. The temporary executable and directory were removed after the probe.
- The generated final verifier was parsed through the Windows PowerShell AST parser: zero errors, one new command pattern, and zero old command patterns.
- Full local execution of `tests/installer/test_package_artifacts.py` returned 47 passed in 2.51 seconds.
- Ruff check and format check passed for both changed files. `git diff --check` passed, neither changed file has a UTF-8 BOM, and the detached worktree remained clean.
- The supplied Actions run `29676647914` is the successful exact-head one-job roadmap-status workflow. Live PR checks establish the broader claim: all 17 exact-head jobs passed, including unit/GStreamer/NATS, Docker cleanroom, Windows installer compile/tests, frontend/e2e, lint/type/format, docs rendering, accessibility, security, operator build, and roadmap status.

## Five-dimension result

- **Correctness and security:** native stderr is suppressed below PowerShell while native exit status remains available; fixed command text introduces no variable interpolation or user-controlled command input.
- **UX:** the proof kit can proceed past WSL enumeration on the clean-machine condition it is designed to inspect. No product UI changed.
- **Documentation:** the generated verifier's adjacent explanation matches the executed failure and fix. No public release documentation changed.
- **Tests:** the focused control discriminates parent from head, the whole package-artifact module passes, and independent runtime execution validates the behavior that the committed structural test protects.
- **Runtime:** the exact old and new invocation forms were exercised under Windows PowerShell with a deterministic native-stderr/no-WSL surrogate. The complete generated verifier and installer were not run on a new pristine guest by the auditor.

## Confidence and boundaries

- **Class 1:** exact detached checkout, complete two-file delta and final-state review, governance reconciliation, and live PR/check inspection.
- **Class 2:** exact head/parent control discrimination, package-artifact regressions, generated-script AST validation, ruff, formatting, BOM, and diff hygiene.
- **Class 3:** Windows PowerShell execution of the exact enumeration forms with a PATH-isolated failing `wsl.exe` surrogate.
- **Hosted CI:** 17 exact-head PR checks are green. Hosted CI and the Docker cleanroom are not the required exact-artifact pristine-Windows gauntlet.
- **Not established:** a newly built artifact containing this verifier; execution of that artifact's complete verifier on a pristine Windows guest; real install, Ollama/model provisioning, inference, rehearsal/playback, returning-Setup/UAC behavior, or cold-reboot recovery.
- The coder-reported clean-VM partial execution is corroborating evidence only. The synthetic GUI wait hung, the guest was reverted, and the full product run was not completed.
- The pre-fix banked artifact cannot inherit this source verdict. It must not be used to satisfy the charter proof bar. Rebuild first, then run the entire gauntlet on that exact artifact.
- No CivicCast source edit, coder-branch edit, merge, tag, release, artifact build, clean-machine installation, LPM action, or public-release change was performed by the auditor.

## Governance and final rationale

The expected-red parent execution and the independent Windows PowerShell native-stderr probe establish that CC-RC17-004 is real and that this exact change closes it. Final source, generated output, focused regressions, hygiene checks, and exact-head hosted checks agree. No functional finding remains in the round-3 scope.

`AUDIT_PASS` binds only to CivicCast SHA `9aceafabaf65d819f21f136442bec6037b7a16e9`. It permits a fresh artifact to be built for the owner-scheduled pristine-machine gauntlet. It does not validate the already banked artifact and does not authorize a merge to CivicCast `main`, a tag, a release, or an LPM handoff. Scott Converse remains owner and tie-breaker.
