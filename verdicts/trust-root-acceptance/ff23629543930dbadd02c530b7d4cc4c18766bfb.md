# Trust-Root Acceptance Slice Audit Verdict

- **Verdict:** `AUDIT_PASS`
- **Slice:** `trust-root-acceptance`
- **Requested gate:** owner activation of claims D5/D6 trust root and authority-record-v1 external resolution
- **CivicCast repository:** `https://github.com/scottconverse/civiccast`
- **Audited SHA:** `ff23629543930dbadd02c530b7d4cc4c18766bfb`
- **Base / merge base:** `ddc5e75985af765be7ecda88172d691a4fec5e34`
- **Branch / PR:** `claude/trust-root-acceptance` / CivicCast PR #298, targeting `program/native-windows`
- **Codex thread ID:** `019f74cb-7d7d-7762-8246-f666f424c91d`
- **Audit date:** 2026-07-18 (America/Denver)
- **Auditor environment:** DESKTOP-VBMA6O5; Microsoft Windows 11 Pro 10.0.26200 x64; PowerShell 5.1.26100.8875; Python 3.12.10; pytest 9.0.3; Git 2.54.0.windows.1; uv 0.11.23; ruff 0.15.12 from the preserved audit virtual environment
- **Execution posture:** `sandbox=danger-full-access`; `approval_policy=never`
- **Detached worktree:** `C:\Users\Scott\Desktop\CODE\_audit-worktrees\civiccast-trust-root-acceptance-ff236295`
- **Worktree state:** exact, detached, and clean at binding and verdict writing
- **Activity relay:** <https://github.com/scottconverse/civiccast/pull/298#issuecomment-5011808954>
- **Hosted activation run:** <https://github.com/scottconverse/civiccast/actions/runs/29649938664>
- **Owner authorization:** Scott Converse's in-session 2026-07-18 acceptance supplied in the audit request; the CivicCast owner register records the accepted and parked decisions at `.agent-runs/native-windows/specs/README.md:19`

## Executive result

The owner-accepted trust root is active at this exact CivicCast SHA. The final product pin matches the current two-principal Beelink `allowed_signers` blob, retains the auditor-ratified authority-record-v1 governance blob, and binds the final verifier, workflow, and trust-root bytes across all six registry entries. The workflow now performs a full clone of the canonical audit-control repository before external resolution.

The auditor created the two required evidence records and then, in a later signed commit, the two create-only authority records. Both records use the exact source/claim/control paths, bind all typed registry inputs, and state the historical spike boundaries. Local external mode passes with absolute and relative audit-control paths. Exact-head hosted CI cloned audit-control after the authority push and passed both same-run and external-evidence modes.

The relative-directory and missing-clone defects are closed. Both focused controls pass at the audited head; the exact parent verifier independently reproduces the double-join failure and raw `NotADirectoryError`. No blocking functional defect remains. Current registry prose still describes owner acceptance and authority creation as pending; that is a batched Minor wording/state-description issue and does not alter resolver behavior.

## Activation records authored

- Evidence commit `3582b9f89f20552bfe6f68b5ab03e9dafcc44e19` is validly signed by `codex-auditor@civiccast-program` and introduces exactly two canonical JSON paths. Their blobs are `6bc0d634adb35eb592a3ff8688bf177bb5c866d8` for `native-decision-gate` and `7c9e8525768d86b2d4d6a61fb27d00b4ace74927` for `session0-service-broadcast`.
- Authority commit `38ab4daec4b7b6f469c4c869554ea9a3bc21659a` is validly signed by the pinned auditor identity and introduces exactly two canonical authority paths. Each names evidence commit `3582b9f8...` and its corresponding exact blob.
- First-parent history contains one touch for each of the four paths. The evidence commit is an ancestor of the authority commit, and later unrelated audit-control commit `e6082a2a...` did not touch any record path.
- The decision-gate authority supports only the recorded confidence-class-3 single-developer-machine hot-swap, hard-kill/relaunch, and three-process multichannel spike for the three bound engine modules. No TS payload was retained for independent replay.
- The session-0 authority supports only the recorded single-machine temporary NSSM/LocalSystem spike, with the committed run-log/Security-4624/NTFS observation narrative and graph. The raw service log, Security export, and TS payload are not retained here, and the experiment was not re-executed during activation.
- Neither record establishes a production service wrapper, production recovery policy, packaging, soak, independently pristine machine, live peer, LPM station-device, or physical-hardware proof.

## Severity rollup

- Blocker: 0
- Critical: 0
- Major: 0
- Minor: 1 batched prose/state-description item
- Nit: 0

## Finding

### CC-TRA-001 - Minor - Registered claim prose still describes the activated trust root as pending

- `docs/claims/claims.yaml:367` and `docs/claims/claims.yaml:470` say the authority records do not exist yet. The claim descriptions at `docs/claims/claims.yaml:380` and `docs/claims/claims.yaml:482` still say they are pending owner trust-root acceptance.
- At this source SHA, `docs/claims/trust-root.yaml:18` is true, and the canonical records now exist and resolve. The stale text is conservative and does not drive verification; resolution depends on the trust-root value and canonical Git history.
- Under the owner's severity calibration this is batched wording/state-description drift. Smallest closure: update the current registry comments/claim descriptions in a later relevant claims-maintenance round without rewriting the historical spike documents, then re-pin normally.

## Proofs re-run and evidence inspected

- Live PR #298 was open, non-draft, mergeable, targeted `program/native-windows` at `ddc5e759...`, and named exact head `ff23629543930dbadd02c530b7d4cc4c18766bfb`. GitHub reported a valid source-commit signature.
- Exact detached checkout; complete seven-file base-to-head diff and final cross-file state reviewed; `git diff --check` and final status clean.
- Owner acceptance is active at `docs/claims/trust-root.yaml:18`. The expected signer blob at line 37 equals audit-control `keys/allowed_signers` blob `0ddf3eb4bbef564f626012a116ccabed0c4f4d65`. The pinned ratifying commit at line 51 resolves `AUTHORITY_RECORDS.md` to the pinned blob `d6944d08d5870a60df5ae912aa8cfc62c2f4e047`.
- Final CivicCast blobs are verifier `2fc636c76fd7a0bf5d75c97aef9efe8ea43947d2`, workflow `8e2d9cf20409ec984570f16f3f1bc03c0f28eb82`, and trust root `cd452e36f65223405cd6e33ce65efa3c42a639bc`. Every one of the six registry entries binds those exact values; no stale value remains.
- Real committed registry sweeps returned zero marker violations, zero blob-drift violations, and zero claim-capability-token violations.
- Workflow inspection confirmed the full canonical audit-control clone at `.github/workflows/ci-test.yml:646` and the external verifier's exact source-SHA input. The five-job workflow inventory and three-producer claims dependency remain unchanged.
- Missing/non-repository audit-control handling returns the named fail-closed violation at `scripts/policy/check_claims_evidence.py:2087` before signature resolution. Relative audit-control CLI input is resolved once at the argument boundary at `scripts/policy/check_claims_evidence.py:2145`.
- The focused relative-dir and real-file missing-clone tests at `tests/policy/test_claims_evidence.py:3274` and `tests/policy/test_claims_evidence.py:1915` returned 2 passed and 216 deselected in 17.19 seconds. Their first launch exceeded a 30-second wrapper and produced no result; the completed relaunch is the recorded outcome.
- An in-memory exact-parent reconstruction at `ddc5e759...` reproduced the relative-path double join with Git's `could not open` failure. Running the exact parent verifier bytes against a missing clone exited 1 with a raw `NotADirectoryError` traceback. Both failures disappear at the audited head.
- `python -m pytest tests/policy/test_claims_evidence.py -q` returned 218 passed in 120.19 seconds. Its first launch exceeded a two-minute wrapper and produced no result; the completed relaunch is the recorded outcome.
- The first broad run returned 529 passed, 20 skipped, and one Windows Git `hash-object` temporary-file stat error after 206.33 seconds. The exact failed parameter immediately passed alone in 2.60 seconds. A complete broad rerun then returned 530 passed and 20 skipped in 163.82 seconds. The skips were one Linux-cleanroom condition and 19 local Docker-gated Postgres cases.
- Ruff reported all checks passed for `scripts/policy` and `tests/policy`. No BOM was present in the seven changed files. The diff contains expected existing Unicode prose punctuation; no ASCII-only claim was made. `git diff --check` was clean.
- Local external mode against the signed records returned `claims-evidence: PASS` with both an absolute audit-control path and the relative `..\..\civiccast-audit-control` path.
- Exact-head GitHub Actions run `29649938664` succeeded in all five jobs. It recorded 5,978 passed and 91 skipped in the unit job; Postgres restore 20 passed and zero skipped; WS4 pure suite 107 passed and zero skipped; Windows-native 168 passed, zero skipped, zero failed; GStreamer 13 passed and one expected skip. Same-run claims verification passed.
- The hosted claims job checked out exact SHA `ff236295...`, cloned audit-control at 2026-07-18T15:49:46Z, after authority commit `38ab4dae...` was pushed, then printed `claims-evidence: PASS` in external mode and `external-evidence verifier passed.` This is the activation proof requested by the owner.

## Confidence and boundaries

- **Class 1:** exact detached checkout, complete diff/final-state review, owner-register/pin/record/history/signature/blob reconciliation, live PR and CI log inspection.
- **Class 2:** focused and full local tests, exact-parent negative reconstructions, real-registry sweeps, local absolute/relative external resolution, ruff, and hygiene checks.
- **Class 3 / hosted CI:** exact-head Docker/Postgres, GStreamer, NATS, Windows-native, same-run claims, and live external authority resolution. Hosted CI is not an independently pristine product installation.
- Authority activation preserves the historical confidence of the two underlying spikes; it does not rerun or elevate them.
- This slice has no UI surface; UI/UX, browser, accessibility, responsive, and visual QA are non-applicable.
- No CivicCast source/coder-branch edit, merge, tag, release, rc-line action, new ADR decision, new owner acceptance, LPM action, or cutover was performed. Audit-control writes were limited to the requested evidence, authority, and verdict records.

## Governance and final rationale

The accepted pin matches the live signer/governance bytes, the two external claims now resolve through canonical create-only records signed by the auditor, and both local and hosted execution prove the activated path. The two latent first-live-run defects have effective controls that demonstrably fail against the parent implementation. The broader regressions and exact-head CI are clean, with only the stated environmental skips and one non-reproducing local Git subprocess anomaly fully disclosed.

`AUDIT_PASS` binds only to CivicCast SHA `ff23629543930dbadd02c530b7d4cc4c18766bfb`. Under the standing owner authorization, this exact SHA may merge only into `program/native-windows`, never CivicCast `main`. This verdict authorizes no tag, release, rc-line action, additional owner decision, LPM action, or cutover. Scott Converse remains owner and tie-breaker. No escalation beyond this scoped audit is warranted.
