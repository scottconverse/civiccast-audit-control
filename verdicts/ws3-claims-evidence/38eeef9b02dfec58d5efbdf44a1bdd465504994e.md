# WS3 Claims-Evidence Round-8 Audit Verdict

- **Verdict:** `AUDIT_PASS`
- **Slice:** `ws3-claims-evidence`
- **Requested gate:** Charter program-sequence item 3, claims-evidence rule; ADR-0021 and trust-root owner acceptance remain separate owner-gated actions
- **CivicCast repository:** `https://github.com/scottconverse/civiccast`
- **Audited SHA:** `38eeef9b02dfec58d5efbdf44a1bdd465504994e`
- **Merge parents:** `ccfaa5dc3c974260a48eb7b8d6331159922ad7b1` (round-8 fix) and `d8f27c3ddf3bceef9ba58466002716f85e370e9a` (`program/native-windows` after WS4 integration)
- **Round-7 audited ancestor:** `3f79a0201a69e5bd13d4c66f3bd99cdad45f94cb`
- **PR base / merge base:** `d8f27c3ddf3bceef9ba58466002716f85e370e9a`
- **Branch / PR:** `claude/ws3-claims-evidence` / CivicCast PR #293, targeting `program/native-windows`
- **Codex thread ID:** `019f74cb-7d7d-7762-8246-f666f424c91d`
- **Audit date:** 2026-07-18 (America/Denver)
- **Auditor environment:** DESKTOP-VBMA6O5; Microsoft Windows 11 Pro 10.0.26200 x64; PowerShell 5.1.26100.8875; Python 3.12.10; pytest 9.0.3; Git 2.54.0.windows.1; uv 0.11.23; ruff 0.15.12 from the preserved audit virtual environment
- **Execution posture:** `sandbox=danger-full-access`; `approval_policy=never`
- **Detached worktree:** `C:\Users\Scott\Desktop\CODE\_audit-worktrees\civiccast-ws3-claims-evidence-38eeef9b-r8`
- **Worktree state:** exact, detached, and clean at binding and verdict writing
- **Activity relay:** <https://github.com/scottconverse/civiccast/pull/293#issuecomment-5011182671>
- **Heartbeat result:** start, all phase transitions, the announced broad-suite run, its wrapper-timeout relaunch, and verdict writing were relayed; no relay failure occurred
- **Prior verdict:** round 7 `CHANGES_REQUIRED` at `verdicts/ws3-claims-evidence/3f79a0201a69e5bd13d4c66f3bd99cdad45f94cb.md`

## Executive result

Round 8 closes CC-WS3-008. Unsupported Markdown-list analysis now runs before both block-level early exits, so a marker-only unsupported run and a strong-token-only unsupported run each receive the required location-bearing fail-closed violation. The second case also retains the generic unmarked-token violation. Both new controls pass at the audited head and independently fail for the exact round-7 behaviors when executed against the round-7 verifier bytes.

The merge resolution also preserves both slices' workflow additions. The final workflow contains the WS4 pure-suite floor and Windows-native job together with the WS3 producer uploads and claims-verifier. Its live five-job set equals the workflow inventory, while `claims-verifier.needs` remains exactly the three registered evidence producers. Final workflow, workflow-contract, and verifier blobs agree with every registry binding, and the unmodified real registry returns zero marker and blob-drift violations.

No blocking functional defect was found. The previously recorded stale historical command transcript remains a batched Minor under the owner's severity decision and does not withhold exact-SHA merge authority.

## Prior-finding reconciliation

| Finding | Status at `38eeef9b` | Result |
|---|---|---|
| CC-WS3-001 through CC-WS3-007 | Closed | Prior lexical, pending-owner, identity, artifact-routing, multi-module, registry-shape, and evidence-input controls remain intact. |
| CC-WS3-008 | Closed | Both one-sided unsupported-list triggers now run before the early exits and fail closed with file and line. |
| CC-WS3-009 | Open, batched Minor | The historical round-6 external-mode transcript still names the stale round-5 source SHA. It does not alter the exact-head round-8 result. |

## Severity rollup

- Blocker: 0
- Critical: 0
- Major: 0
- Minor: 1 batched historical-transcript precision item
- Nit: 0

## Findings

### CC-WS3-009 - Minor - Historical round-6 transcript retains a stale source SHA

- `.agent-runs/native-windows/ws3-claims-evidence/evidence/README.md:1216` still records the historical round-6 external-mode command with `ab39e927...` rather than that round's audited head.
- This is a historical evidence-transcript precision issue. The exact-head round-8 local invocation and CI invocation both used `38eeef9b...`, exited 2 in external mode, and reported `PENDING_OWNER_ONLY=true` for only the two owner-pending authority records.
- Under the owner's severity calibration this remains batched Minor and does not block this audit.

## Proofs re-run and evidence inspected

- Live PR #293 was open, non-draft, mergeable, targeted `program/native-windows` at `d8f27c3d...`, and named exact head `38eeef9b02dfec58d5efbdf44a1bdd465504994e`. GitHub reported a valid commit signature.
- Exact detached checkout; both merge parents resolved as requested; `git diff --check` and final status were clean.
- The complete round-8 fix diff, final verifier/caller/test state, both parent-to-merge differences, and Git's remerge diff were reviewed. The only textual merge conflict was the workflow append point. The final resolution keeps the WS4 pure-suite assertion at `.github/workflows/ci-test.yml:264`, the WS3 producer uploads at `.github/workflows/ci-test.yml:309`, the Windows-native job at `.github/workflows/ci-test.yml:482`, and the claims-verifier's unchanged three-producer dependency at `.github/workflows/ci-test.yml:575`.
- Unsupported-shape analysis is invoked at the start of each Markdown block at `scripts/policy/check_claims_evidence.py:928`, its per-span token-or-marker trigger is at `scripts/policy/check_claims_evidence.py:932`, and both former bypassing exits now follow at `scripts/policy/check_claims_evidence.py:942` and `scripts/policy/check_claims_evidence.py:945`. Python documents still use `_capability_sub_blocks` at `scripts/policy/check_claims_evidence.py:966`.
- The marker-only and token-only committed controls at `tests/policy/test_claims_evidence.py:3226` and `tests/policy/test_claims_evidence.py:3247` both passed. `pytest -k "ws3r7 or ws3r8"` returned 16 passed and 201 deselected in 5.03 seconds.
- An independent in-memory reconstruction loaded the exact verifier bytes from round-7 SHA `3f79a020...` and invoked both new tests. The marker-only case failed with `[]`; the token-only case failed with only the generic unmarked-token violation. This proves both tests can detect the claimed regression.
- The real committed registry returned zero marker violations and zero blob-drift violations. The final verifier blob is `657c2801f3310f5d2e51ab9f294dce44ba75c8b5`, the workflow blob is `eeb18574b2f7f29e6a4b890f3b7a24a28bec961b`, and the workflow-contract blob is `05051b40d56f1d8a248c39666b7b978db5462094`; all six registry entries bind those exact values at `docs/claims/claims.yaml:130` through `docs/claims/claims.yaml:518`, with no prior pin remaining.
- YAML loading found exactly five final workflow jobs. That set equals `workflow_job_inventory` at `docs/claims/workflow-contract.yaml:39`; `windows-native` is inventory-only at line 43, and the three `expected_producers` at line 46 exactly equal the claims-verifier's `needs` set.
- `python -m pytest tests/policy/test_claims_evidence.py -q` returned 217 passed in 76.89 seconds.
- The first launch of the broad suite was terminated by its command wrapper after roughly 14 seconds and produced no test result. The identical command was relaunched with the announced three-minute allowance; `python -m pytest tests/policy tests/dr -q` returned 529 passed and 20 skipped in 142.61 seconds. The skips were one Linux-cleanroom condition and 19 local Docker-gated Postgres cases.
- Ruff reported all checks passed for `scripts/policy` and `tests/policy`. No BOM was present in the five scoped machinery files, all 235 added lines in the audited round-7-to-round-8/merge machinery diff were ASCII, and `git diff --check` was clean.
- Local external-evidence mode used exact source SHA `38eeef9b...`, exited 2, and emitted `PENDING_OWNER_ONLY=true` only for `native-decision-gate` and `session0-service-broadcast`, matching the unaccepted trust-root state.
- Exact-head GitHub Actions run `29642895177` succeeded in all five jobs. The unit job recorded 5,977 passed and 91 skipped; its Postgres restore floor recorded 20 passed and zero skipped, and its WS4 pure-suite floor recorded 107 passed and zero skipped. The Windows-native job recorded 168 passed, zero skipped, zero failed, floor 168. The GStreamer job recorded 13 passed and one expected skip. The same-run claims verifier printed `claims-evidence: PASS`; external mode used exact source SHA `38eeef9b...` and emitted `PENDING_OWNER_ONLY=true`.

## What is working

- CC-WS3-008's two one-sided unsupported-list bypasses are closed without changing the established supported-item locality or Python-docstring path.
- The round-7 precision behavior remains: an unsupported run with neither a strong token nor a marker does not produce the unsupported-form violation.
- Merge-resolution drift is covered through the front-door blob and job-inventory contracts, and those controls agree with the final five-job workflow.
- WS4's Linux pure-suite floor and real-Windows job coexist with WS3's producer/consumer artifact chain; the Windows job is correctly not promoted to a claims-evidence producer.

## Confidence and boundaries

- **Class 1:** exact detached checkout, complete scoped and merge-resolution review, final-state/source tracing, live PR/run/signature inspection, registry-pin reconciliation, and governance reconciliation.
- **Class 2:** focused and full local test execution, exact-parent expected-red reconstruction, real-registry execution, YAML contract comparison, ruff, and hygiene checks in the detached worktree.
- **Class 3 / hosted CI:** exact-head CI executed Docker/Postgres, GStreamer, NATS, artifact producer/consumer, same-run/external claims verification, the WS4 pure-suite floor, and the real Windows probe job. This is hosted evidence, not a local Beelink Docker or independently pristine Windows proof.
- This slice has no UI surface; UI/UX, browser, accessibility, responsive, and visual QA are non-applicable.
- No new session-0, physical hardware/LPM, station-device, independently pristine-machine, or soak proof is granted. The owner's testing-policy bounds were applied.
- No CivicCast source/coder-branch edit, merge, tag, release, rc-line action, ADR owner approval, trust-root owner acceptance, or cutover was performed.

## Governance and final rationale

The round-8 ordering fix reaches both literal one-sided triggers, the controls demonstrably fail against the prior verifier, and the complete claims and broader regression suites remain green. The merge resolution preserves the intended WS3 and WS4 workflow surfaces, while exact blob pins and job-inventory checks close the machinery-drift paths. CC-WS3-008 is therefore closed and no blocking functional finding remains.

`AUDIT_PASS` binds only to CivicCast SHA `38eeef9b02dfec58d5efbdf44a1bdd465504994e`. Under the standing owner authorization, this exact SHA may merge only into `program/native-windows`, never CivicCast `main`. This verdict authorizes no tag, release, rc-line action, ADR owner approval, trust-root acceptance, or cutover. Scott Converse remains owner and tie-breaker.
