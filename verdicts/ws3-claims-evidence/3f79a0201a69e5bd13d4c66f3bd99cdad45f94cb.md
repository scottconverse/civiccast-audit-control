# WS3 Claims-Evidence Round-7 Audit Verdict

- **Verdict:** `CHANGES_REQUIRED`
- **Slice:** `ws3-claims-evidence`
- **Requested gate:** Charter program-sequence item 3, claims-evidence rule; ADR-0021 and trust-root owner acceptance remain separate owner-gated actions
- **CivicCast repository:** `https://github.com/scottconverse/civiccast`
- **Audited SHA:** `3f79a0201a69e5bd13d4c66f3bd99cdad45f94cb`
- **Parent:** `99b4ca42e7b88d2ea2013eb306a8f356defd1f8d` (the round-6 audited head)
- **PR base / merge base:** `0d288df53f3d482e0e6068ef6032fa3b2d6cd5d5`
- **Branch / PR:** `claude/ws3-claims-evidence` / CivicCast PR #293, targeting `program/native-windows`
- **Codex thread ID:** `019f74cb-7d7d-7762-8246-f666f424c91d`
- **Audit date:** 2026-07-18 (America/Denver)
- **Auditor environment:** DESKTOP-VBMA6O5; Microsoft Windows 11 Pro 10.0.26200 x64; PowerShell 5.1.26100.8875; Python 3.12.10; pytest 9.1.1; Git 2.54.0.windows.1; uv 0.11.23; ruff 0.15.12 from the preserved audit virtual environment
- **Execution posture:** `sandbox=danger-full-access`; `approval_policy=never`
- **Detached worktree:** `C:\Users\Scott\Desktop\CODE\_audit-worktrees\civiccast-ws3-claims-evidence-3f79a020-r7`
- **Worktree state:** exact, detached, and clean at binding and verdict writing
- **Activity relay:** <https://github.com/scottconverse/civiccast/pull/293#issuecomment-5010966776>
- **Heartbeat result:** start, phase transitions, the announced 134-second full-suite run, and verdict writing were relayed; no relay failure occurred
- **Prior verdict:** round 6 `CHANGES_REQUIRED` at `verdicts/ws3-claims-evidence/99b4ca42e7b88d2ea2013eb306a8f356defd1f8d.md`

## Executive result

Round 7 closes the round-6 sibling- and hierarchy-borrowing cases for `+`, ordered-dot, ordered-paren, blockquoted, and deeper-nested supported Markdown items. All 14 committed round-7 controls pass, all prior regressions remain passing, the unmodified real registry has zero marker and blob-drift violations, and all six verifier pins match the final verifier blob.

The requested fail-closed half is incomplete. `scan_marker_violations` skips an entire governed block when it has no strong token before it invokes `_markdown_item_locality`. An unsupported list item containing a claim marker but no strong token therefore returns zero violations, even though the acceptance criterion says either a strong token or a claim marker in an unsupported run must emit the location-bearing unsupported-form violation. The symmetric unsupported strong-token case with no marker elsewhere emits only the older generic unmarked-token message and returns before the required unsupported-form analysis.

This is a reachable fail-open claim-association path, not wording precision. CC-WS3-008 remains Major under the owner's severity calibration, so the exact SHA does not receive merge authority.

## Prior-finding reconciliation

| Finding | Status at `3f79a020` | Result |
|---|---|---|
| CC-WS3-001 through CC-WS3-007 | Closed | Prior lexical, pending-owner, identity, artifact-routing, multi-module, registry-shape, and evidence-input controls remain intact. |
| CC-WS3-008 | Open, Major | Supported multi-form item locality closes; unsupported-form fail-closed analysis is still bypassed by the scanner's block-level early exits. |
| CC-WS3-009 | Open, batched Minor | The historical round-6 external-mode transcript still names the stale round-5 source SHA. It does not alter the exact-head round-7 result. |

## Severity rollup

- Blocker: 0
- Critical: 0
- Major: 1
- Minor: 1 batched historical-transcript precision item
- Nit: 0

## Findings

### CC-WS3-008 - Major - Unsupported claim-marker runs bypass the promised fail-closed analysis

- **Dimension:** claim-to-evidence association / Markdown structure / functional false pass
- **Code evidence:** `_GENERIC_LIST_START_RE` recognizes the unsupported letter-ordered shape at `scripts/policy/check_claims_evidence.py:783`, and `_markdown_item_locality` records unsupported runs at `scripts/policy/check_claims_evidence.py:855`. However, `scan_marker_violations` returns to the next block when no strong token exists at `scripts/policy/check_claims_evidence.py:914`, before unsupported-run analysis begins at `scripts/policy/check_claims_evidence.py:938`. Its no-marker path likewise continues at `scripts/policy/check_claims_evidence.py:917` before that analysis.
- **Executed falsification A:** a governed `docs.md` containing one letter-ordered item with ordinary prose and a valid claim marker, with a matching registry entry and anchor but no strong token, returned `[]`. The required unsupported-form violation did not fire.
- **Executed falsification B:** a governed `docs.md` containing one letter-ordered strong claim and no marker anywhere produced only the older generic unmarked-token violation. It did not name `docs.md:1` or `unsupported list form for locality analysis` because the no-marker early exit ran first.
- **Test gap:** the committed fail-closed test at `tests/policy/test_claims_evidence.py:3173` includes both a strong token and a sibling marker in the same paragraph, so it passes through both early gates. Its precision companion at `tests/policy/test_claims_evidence.py:3195` has neither a token nor a marker in the unsupported run. Neither exercises the two one-sided trigger conditions required by the acceptance criterion.
- **Consequence:** a claim marker placed in an unsupported Markdown list form can still be accepted without any locality analysis. The checker can exit successfully despite content the round-7 contract explicitly classifies as unanalyzable and non-authorizing.
- **Smallest acceptance criterion:** run unsupported-form detection before the block-level strong-token and marker early exits, or otherwise guarantee both one-sided triggers reach it. Add committed controls for an unsupported run containing only a claim marker and an unsupported strong-claim run with no marker elsewhere; each must name the governed file, exact line, and `unsupported list form for locality analysis`. Preserve all 14 current round-7 controls, Python-docstring behavior, real-registry sweeps, marker-removal reconstruction, and prior parser tests.
- **Blast radius:** all enforced Markdown governed documents and future claim markers authored in list forms outside the supported set; Python governed docstrings are not implicated.

### CC-WS3-009 - Minor - Historical round-6 transcript retains a stale source SHA

- `.agent-runs/native-windows/ws3-claims-evidence/evidence/README.md:1214` still records the round-6 external-mode command with `ab39e927...` rather than that round's audited head.
- This remains a historical evidence-transcript precision issue. Exact-head round-7 CI invoked external mode with `3f79a020...`, exited 2, and emitted `PENDING_OWNER_ONLY=true` for only the two owner-pending controls.
- Under the owner's severity calibration this remains batched Minor and does not independently trigger another round.

## Proofs re-run and evidence inspected

- Live PR #293 was open, non-draft, targeted `program/native-windows`, and named exact head `3f79a0201a69e5bd13d4c66f3bd99cdad45f94cb`. GitHub verified the exact commit's SSH signature. PR mergeability reported `CONFLICTING` at verdict time; no merge was attempted.
- Exact detached checkout; parent resolved to `99b4ca42e7b88d2ea2013eb306a8f356defd1f8d`; `git diff --check` and final status were clean.
- The complete parent-to-head diff and final parser/caller/test/pin state were reviewed. The test-file diff is 242 additions and zero deletions.
- `python -m pytest tests/policy/test_claims_evidence.py -q -k ws3r7` returned 14 passed and 201 deselected in 4.30 seconds.
- An independent parent-state reconstruction loaded the exact round-6 verifier bytes and reproduced clean results for the `+`, ordered-dot, ordered-paren, blockquoted, nested-sibling, and parent-inheritance false-pass shapes. The round-7 tests therefore can fail against the prior implementation for the conditions they claim to detect.
- The auditor's two independent unsupported-form controls produced the results described in CC-WS3-008 above.
- `python -m pytest tests/policy/test_claims_evidence.py -q` returned 215 passed in 76.18 seconds.
- `python -m pytest tests/policy tests/dr -q` returned 527 passed and 20 skipped in 134.12 seconds. The skips were one Linux-cleanroom condition and 19 local Docker-gated Postgres cases.
- Ruff reported all checks passed for `scripts/policy` and `tests/policy`.
- The real committed registry returned zero marker violations and zero blob-drift violations. All six `verifier[0]` entries contain `6c52493e2f9faa08b2805eef4f9950a3ec5b92e4`, which equals the final verifier's Git blob; no old verifier pin remains.
- No BOM was present in the four changed files; `git diff --check` was clean; all added lines were ASCII.
- Exact-head GitHub Actions run `29640561268` succeeded in all four jobs. The unit job recorded 5,868 passed and 30 skipped; its Postgres restore floor recorded 20 passed, zero skipped, floor 20. The same-run claims verifier printed `claims-evidence: PASS`. External mode used exact source SHA `3f79a020...`, exited 2, and emitted `PENDING_OWNER_ONLY=true` for the two owner-pending records. GStreamer and NATS jobs also succeeded.

## What is working

- Supported Markdown list items now receive separate sub-blocks across all five requested marker families, blockquotes, and tested nesting depths at `scripts/policy/check_claims_evidence.py:786`.
- Strict own-item capability coverage remains in force with no enclosing-marker fallback at `scripts/policy/check_claims_evidence.py:964`.
- The 14 new tests are additive and pair every named residual falsification with an own-marker or precision companion at `tests/policy/test_claims_evidence.py:2972`.
- Python-docstring splitting remains routed through `_capability_sub_blocks`; the round-7 Markdown parser is not used on Python governed docs at `scripts/policy/check_claims_evidence.py:909`.
- The real governed surfaces required no restructuring, and their final registry sweep is clean.

## Confidence and boundaries

- **Class 1:** exact detached checkout, complete scoped diff/final-state review, governance reconciliation, live PR/run/log/signature inspection, pin reconciliation, and independent source tracing.
- **Class 2:** focused and full local test execution, parent-state red reconstruction, independent unsupported-form falsifications, ruff, and real-registry execution in the detached worktree.
- **Class 3 / hosted CI:** exact-head Linux CI executed the Docker/Postgres, GStreamer, NATS, artifact producer/consumer, and same-run/external claims-verifier paths. This is hosted evidence, not local Beelink Docker proof.
- This slice has no UI surface; UI/UX, browser, accessibility, responsive, and visual QA are non-applicable.
- No new session-0, pristine-machine, hardware/LPM, station-device, or soak proof is granted. The owner's testing-policy bounds were applied.
- No CivicCast source/coder-branch edit, merge, tag, release, rc-line action, ADR owner approval, trust-root owner acceptance, or cutover was performed.

## Governance and final rationale

The parse-all-supported-forms half is materially improved and the named round-6 counterexamples are closed. The fail-closed half is not complete because unsupported marker-only content bypasses the new analysis and returns success. That is a functional false pass under the owner's severity calibration, so CC-WS3-008 remains Major.

`CHANGES_REQUIRED` binds only to CivicCast SHA `3f79a0201a69e5bd13d4c66f3bd99cdad45f94cb`. Scott Converse remains owner and tie-breaker. This verdict authorizes no merge. Any future exact-SHA `AUDIT_PASS` may authorize merge only into `program/native-windows`, never CivicCast `main`, and never a tag, release, rc-line action, ADR owner approval, trust-root acceptance, or cutover.
