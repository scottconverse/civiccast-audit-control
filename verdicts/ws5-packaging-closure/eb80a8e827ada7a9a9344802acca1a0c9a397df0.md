# WS5 Packaging-Closure Audit Verdict - Round 7

- **Verdict:** `CHANGES_REQUIRED`
- **Slice:** `ws5-packaging-closure`
- **Requested gate:** `charter-section-7-runtime-packaging`
- **CivicCast repository:** `https://github.com/scottconverse/civiccast`
- **Audited SHA:** `eb80a8e827ada7a9a9344802acca1a0c9a397df0`
- **Merge base:** `ec86aab6ffe2a0071dbccdc20a4e14c4fe65b7c5`
- **Round-6 audited parent:** `6547419625c8722dc267904dc54e07d26421be7f`
- **Round-6 fix commit:** `6809ffc456a8b136e89d9bedda274febe730ec51`
- **Branch:** `claude/ws5-packaging-closure`; the live remote branch resolved to the audited SHA
- **PR / issue relay:** none supplied; relay unavailable from `AUDIT_STARTED`
- **Codex thread ID:** `019f92f6-1889-7c32-ac6e-051d8d822bd3`
- **Prior round-6 thread ID:** `019f92de-60fd-76e2-90f4-a8d1b6b956bd`
- **Audit date:** 2026-07-24 (America/Denver)
- **Auditor environment:** NVIDEABLACKWELL; Windows 11 Pro 10.0.26200 x64;
  PowerShell 5.1.26100.8875; Git 2.54.0.windows.1; Python 3.12.13;
  `uv` 0.11.15; Ruff 0.15.12; mypy 2.0.0; pytest 9.0.3
- **Execution posture:** `sandbox=danger-full-access`; `approval_policy=never`
- **Detached worktree:**
  `C:\Users\scott\AppData\Local\Temp\claude\C--Users-scott-Desktop-CODE\87b1383c-db28-421f-b091-db2630099023\scratchpad\r7-worktree`
- **Worktree state:** detached and clean before verification; after verification,
  only this requested untracked verdict file and the ignored local `.venv`
  junction exist

## Scope

Round 7 reviewed the complete
`6547419625c8722dc267904dc54e07d26421be7f..HEAD` delta: three commits and
seven files comprising the one 016/017 fix commit, the round-7 request and
thread-ledger update, the canonical round-6 verdict copy, and the fresh
cleanroom evidence. The detached checkout and
`origin/claude/ws5-packaging-closure` both resolved to
`eb80a8e827ada7a9a9344802acca1a0c9a397df0`.

The audit reran the exact-SHA gate, the cleanroom identity selftest, and an
independent 12-case identity forgery matrix through the real CLI verification
path. It then cold-read the final slice state for the retracted HEVC instruction
and stale counts, rather than limiting review to the four files in the fix
commit.

No CivicCast implementation, evidence, branch, tag, release, or PR was edited.

## Severity rollup

- Blocker: 0
- Critical: 0
- Major: 0
- Minor: 1
- Nit: 0

## Finding

### CC-WS5-PKG-018 - Minor - A binding correction memo still states the stale 18-file count

- **File:**
  `.agent-runs/native-windows/ws5-packaging-closure/evidence/EXACT-SHA-EVIDENCE.md:116-125`
- **Evidence:** the correction block says the claim that binds the slice was
  re-verified and is unchanged, then says
  `git diff --name-only ec86aab6...HEAD -- '*.py'` currently returns 18 files.
  At the audited SHA, that exact command returns 19 files. The nineteenth is
  `.agent-runs/native-windows/ws5-packaging-closure/evidence/cleanroom_identity.py`.
  The current `audit-request.yaml` correctly says 19, so the two current
  documents contradict one another.
- **What remains true:** `ruff format --check .` reports exactly 38 files, and
  the intersection between those 38 and the 19 slice Python files is empty.
  The error is the count in the evidence memo, not the substantive no-overlap
  conclusion.
- **Consequence:** round 7 explicitly asks for a cold read of every document
  for a stale count and claims the contradiction sweep is clean. A reader of
  the exact-SHA evidence memo still receives a stale value in text presented as
  the binding correction, so that claim is not supported by the final tree.
- **Smallest acceptance criterion:** update the correction block's current
  slice Python count from 18 to 19, retain the measured 38-format-file count
  and zero intersection, and rerun the final-state count/contradiction sweep.
  Historical counts inside clearly labeled original text and prior canonical
  verdicts may remain as history.

## Round-6 finding disposition

- `CC-WS5-PKG-016`: **Closed.** `parse_identity()` now requires
  `type(file_count) is int` and a positive value. `verify()` compares the
  record value against the independently supplied host mount count. The real
  CLI requires `--file-count`, and `cleanroom-gate.sh` passes the count emitted
  by `source_fingerprint.py` over the detached mount. The checked-in selftest
  passes. The auditor's independent CLI matrix accepted the correct count and
  rejected JSON true, false, zero, negative, float, string, null, wrong
  positive, huge positive, duplicate `file_count`, and a truthful record
  checked against a wrong CLI expected count.
- `CC-WS5-PKG-017`: **Closed for its HEVC correction and the request count.**
  `h264-store-extension-check.md` now places a prominent supersession before
  the original text and strikes/replaces the old owner-decision consequence.
  It states that enumeration loaded the Store DLL, the GPU-driver MFT performed
  the encode, and the remaining AMD-iGPU question is hardware capability.
  `audit-request.yaml` now correctly says 19 files. The separately found stale
  count in `EXACT-SHA-EVIDENCE.md` is recorded as `CC-WS5-PKG-018` because it
  lies outside the round-6 finding's named request-file correction and was
  surfaced by the required whole-slice reread.

## Commands and proof rerun

- Checkout binding:
  - `git rev-parse HEAD` ->
    `eb80a8e827ada7a9a9344802acca1a0c9a397df0`
  - `git ls-remote origin refs/heads/claude/ws5-packaging-closure` -> the same SHA
  - `git status --short --branch` -> detached and clean before verification
  - `git log --oneline 65474196..HEAD` -> the three commits in scope
  - `git diff --name-status 65474196..HEAD` -> the seven claimed files
  - `git diff --check 65474196..HEAD` -> no whitespace errors
- `evidence/exact-sha-gate.sh <audited-worktree>` -> **exit 0**:
  - detached mount HEAD: exact audited SHA; mount dirty state empty
  - Ruff: pass
  - mypy: pass, 24 source files
  - native tests: **732 passed**
  - build A/B: 220 manifest files and 75,097,266 bytes each
  - D6: **6 PASS, 0 FAIL, 0 SKIPPED**
  - whole-tree A/B hash:
    `2aa71276d974342355bcc7110cc96ee6223a4ded71eb36d20faed3ca46815819`
  - `SHA256SUMS`:
    `dab7bf0360bb96638a7ebf5a750ac069dbeaf0dfd2a0810623dd7908074aa80c`
  - `runtime-manifest.json`:
    `99143f7ff8039effebbb8f0c5c86a1257dbfb26b4aeb3505c7ab3191754a9eb8`
  - `LICENSE-BOM.md`:
    `8f235f95d99a95c78ea07e0c9ffaf76527d6e6c947e618b5b9d222aa31635b92`
- `cleanroom_identity.py selftest` -> exit 0:
  `identity selftest: every forged record rejected, valid record accepted`
- Independent real-CLI identity matrix -> **12/12 expected outcomes**:
  correct count accepted; true, false, zero, negative, float, string, null,
  wrong positive, huge positive, duplicate, and wrong expected-count cases
  rejected.
- Fresh cleanroom artifact review:
  - `evidence/cleanroom-gate.log` records the sandbox gate passing for
    `5dcab64377e020588a6814f6b3f20d730712d3b3`.
  - That SHA contains the 016 implementation. The subsequent audited-SHA commit
    adds the resulting evidence log only.
  - The live acceptance path passes the independently computed mount
    `FILE_COUNT` to `cleanroom_identity.py verify`; the verifier compares it by
    equality with the parsed record.
  - The cleanroom log records artifact equality and a passing structured
    identity decision; it is not treated as proof for any later source change.
- Final-state contradiction/count sweep:
  - exact slice Python count: **19**
  - Ruff format count: **38**
  - intersection: **0**
  - the corrected HEVC retraction/supersession is visible before retained
    historical text
  - the sweep found `CC-WS5-PKG-018`

## Non-qualifying setup attempt

The first local gate attempt used an ignored `.venv` junction to a different
CivicCast checkout whose environment lacked `pefile`. It failed before it could
qualify: mypy, test collection, builds, and D6 did not pass. That result was not
reported as product evidence. The junction target was inspected, corrected to
the packaging branch's existing environment, its imports were verified, and
the complete gate was rerun from the beginning. Only the later exit-0 run
described above supports this verdict.

## Confidence and unrun checks

- Confidence class 1, static/source inspection: achieved.
- Confidence class 2, unit/integration proof in the detached exact-SHA mount:
  achieved.
- Confidence class 3, developer-machine runtime: achieved through the full
  exact-SHA D6 gate.
- The Windows Sandbox cleanroom gate was not launched again by the auditor.
  The committed fresh run at `5dcab643` was inspected, and the identity
  acceptance path plus forgeries were independently rerun locally.
- Windows Sandbox proof is not promoted to independently pristine
  physical-machine, second-venue, LPM field, service/session-0, or soak proof.
- The owner-moved representative D6 workload was not run; it remains at the
  release gate under `OWNER-DECISION-d6-scope.md`.
- The disclosed egress relay failure is outside this slice and was not rerun.
- Licensing review is engineering/provenance review, not legal advice.

## Source-control actions

- No CivicCast implementation, evidence, branch, tag, release, or PR was
  modified or committed.
- This verdict is written to `r7-verdict.md` in the detached audit worktree as
  requested.
- The identical canonical verdict record is committed and pushed only to the
  owner-controlled `civiccast-audit-control` repository under
  `verdicts/ws5-packaging-closure/eb80a8e827ada7a9a9344802acca1a0c9a397df0.md`.

## Final rationale

`CHANGES_REQUIRED` applies only to
`eb80a8e827ada7a9a9344802acca1a0c9a397df0`.

Both round-6 Minors are substantively closed. The structured identity now
verifies `file_count` end to end and resists the required CLI-path attacks. The
H.264 memo prominently supersedes the retracted HEVC premise and removes the
false Store-extension owner instruction. The exact-SHA gate and the fresh
cleanroom evidence support those closures.

The requested whole-slice reread nevertheless found one current evidence memo
still presenting 18 as the binding slice Python-file count when the live
command returns 19. The no-overlap conclusion remains correct, so this is a
Minor documentation-integrity defect rather than a runtime or packaging
failure. It still prevents the round-7 contradiction-sweep claim from being
accepted as written.

Scott Converse remains the sole owner-decision, merge, beta, tag, release, and
cutover authority.
