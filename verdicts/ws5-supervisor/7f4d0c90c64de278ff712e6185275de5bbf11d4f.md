# WS5 Supervisor Re-Audit Verdict — Round 5

- **Verdict:** `CHANGES_REQUIRED`
- **Slice:** `ws5-supervisor`
- **Requested gate:** slice-merge code correctness for charter section 8 program-sequence item 5 / `spec-supervisor.md`
- **CivicCast repository:** `https://github.com/scottconverse/civiccast`
- **Audited SHA:** `7f4d0c90c64de278ff712e6185275de5bbf11d4f`
- **PR base:** `0d042dd2f6bd3873608c4d257e4e9f1455c910ab`
- **Branch / PR:** `claude/ws5-supervisor` / CivicCast PR #300, targeting `program/native-windows`
- **CI run observed:** `29936899628`, exact head SHA; still in progress when this verdict was recorded
- **Codex auditor thread ID:** `019f8a99-bd79-7ec3-bc87-bdb23994c02d`
- **Prior verdict:** `CHANGES_REQUIRED` at `01d0e5ee14fe9c1a4b7dc2a4e79e412fcce6ca58`, thread `019f8a69-14e5-7d41-8f4f-027109fadeb9`
- **Audit date:** 2026-07-22 (America/Denver)
- **Auditor environment:** NVIDEABLACKWELL; Windows 11 Pro 10.0.26200 x64; PowerShell; uv-managed Python 3.12.13; pytest 9.0.3; ruff 0.15.12; mypy 2.0.0; Git 2.54.0.windows.1; uv 0.11.15
- **Execution posture:** `sandbox=danger-full-access`; `approval_policy=never`
- **Detached worktree:** `C:\Users\scott\Desktop\CODE\_audit-worktrees\civiccast-ws5-supervisor-7f4d0c90-r5`
- **Worktree state:** exact detached checkout; tracked tree clean after audit; ignored audit-local `.venv` present
- **Activity relay:** CivicCast PR #300 comment `https://github.com/scottconverse/civiccast/pull/300#issuecomment-5048588319`

## Executive result

`CHANGES_REQUIRED` is returned for exactly
`7f4d0c90c64de278ff712e6185275de5bbf11d4f`.

The round-4 Critical residual, CC-WS5-007, closes. The auditor drove the actual
`default_dependency_provider()` with an in-memory SQLite `DATABASE_URL` and got
a concrete `ProductionDependencies`; drove the unmodified
`build_production_service_factory()(logger)` default path and got a real
`SupervisorService`; instrumented all five `win_probes` callables plus
`RuntimeOwnerMutex.probe` to raise if called and observed zero construction-time
calls; confirmed the guard holds the exact production callables and one bound
`RuntimeOwnerMutex().probe`; reproduced the clear unset-`DATABASE_URL`
`ValueError`; exercised DB success plus NATS/HTTP fail-closed probes; confirmed
`service-down` is a real `AlertConditionKind`; and compared the complete
`build_production_service` AST node with the prior SHA byte-for-byte.

CC-WS5-008 remains the owner/governance Major. The two claims still bind the old
`worker.py` blob, the CivicCast trust root still pins the old audit-control
`allowed_signers` blob, and live audit-control still lacks the Blackwell auditor
key. The claims suite remains intentionally red on the two first-order blob
violations. Scott must make and record the claim disposition, trust-root re-pin,
and key-registration decisions.

CC-WS5-008 is not, however, the sole remaining issue in the current audit
surface. Required artifact/status reconciliation found three coder-correctable
truth defects: PR #300 still names a retired head and describes the now-built
provider as missing; `PENDING.md` simultaneously calls the CC-WS5-012 liveness
wedge fixed and unfixed; and the generated build log omits the real TSDuck
`--deselect` argument from its displayed command while reporting an `Overall:
PASS`. The last defect matters because an actual unfiltered egress run and a
focused retry both fail on that unchanged real-boundary test; the explicitly
deselected panel passes.

These are not implementation regressions in the remediated provider, but two of
them are committed source/evidence defects and therefore require a corrected
CivicCast SHA. The verdict cannot become the owner-only `BLOCKED` state at this
head.

This verdict authorizes no merge, tag, release, install, service mutation,
selector mutation, interlock mutation, or cutover. Scott Converse remains owner
and tie-breaker. A corrected source SHA requires a fresh audit and inherits none
of this verdict.

## Severity rollup

- Blocker: 0
- Critical: 0
- Major: 1
- Minor: 3
- Nit: 0

## Findings

### CC-WS5-008 — Major — governed claims and trust root remain invalid (owner gate)

`docs/claims/claims.yaml` binds `civiccast/egress/gst/worker.py` at
`f00dba1163e24e501184ed517d3228a8293ae70f`; the audited tree contains
`e8a9b621f18d4134ffc93ffe34247dcfdb3ae1d0`. Both
`native-decision-gate` and `session0-service-broadcast` therefore remain invalid.

The independent claims suite returned `213 passed, 10 failed`. Its first-order
failures were exactly the two governed worker-blob violations; the other eight
failures cascade because clean-registry and isolated-mutation tests encounter
that known drift first. Direct external-evidence mode exited 1 with exactly the
same two violations.

Separately, `docs/claims/trust-root.yaml:37` expects audit-control
`keys/allowed_signers` blob
`0ddf3eb4bbef564f626012a116ccabed0c4f4d65`, while live audit-control main
contains `29039525d3db5e37b6f8cd77578586212f612f3c`. The live file includes both
Claude coder keys and the Beelink auditor key, but not the Blackwell auditor key
used here, fingerprint
`SHA256:CTaM3h09QH5SeJEhGCgq2pM46h7mz3AHcM+g7scP1WA`.

**Scope disposition:** owner/governance-gated. Scott must choose whether the two
claims are narrowed/re-attested or the changed worker input is unbound; authorize
the new trust-root pin; and register the Blackwell `codex-auditor` key. A blind
blob bump is not sufficient.

**Blast radius:** claims-verifier CI, audit authority resolution, future auditor
signature verification, and the truthfulness of the native decision-gate and
session-0 evidence.

### CC-WS5-014 — Minor — PR #300 status body is stale again

The live PR body names `db2565126ef6a6a1a90a6236d92ef080041c0a0f` as HEAD,
reports 527 native tests and 44 blob bindings, asks for the round-3 audit, and
lists the concrete production dependency provider plus same-handle client
hardening as still VM-bound implementation work. Live PR head is
`7f4d0c90c64de278ff712e6185275de5bbf11d4f`; the provider and same-handle path
are committed and unit-proven. This is the same class of public status drift
previously tracked as CC-WS5-014.

**Required acceptance:** update the PR body to the exact current head, current
proof counts, current verdict/finding state, and the precise remaining VM/owner
boundaries. Preserve the no-merge statement.

### CC-WS5-018 — Minor — `PENDING.md` still contradicts itself about F-REV-6

`.agent-runs/native-windows/ws5-supervisor/evidence/PENDING.md:215-235` says the
CC-WS5-012 blocked-state recovery liveness defect was fixed in round 4. Lines
237-252 then repeat the obsolete disclosure that the same resume-withhold edge
is pre-existing, deliberately not fixed, and a candidate for the next cleanup;
the proposed fix is the state transition the earlier section says already
landed. The artifact therefore still preserves both mutually exclusive states.

**Required acceptance:** remove or rewrite the obsolete duplicate so one final
section accurately distinguishes the fixed pure logic from the still-unrun live
three-child/SCM proof.

### CC-WS5-019 — Minor — generated evidence does not display the command it executed

`.agent-runs/native-windows/ws5-supervisor/evidence/run_evidence.py:171-183`
executes the egress panel with
`--deselect tests/egress/test_ts_relay.py::test_real_tsp_relay_makes_cc_continuous_across_session_reset`,
but labels it only `python -m pytest tests/egress -q`. The generated log repeats
that incomplete command at line 101, then reports `685 passed, 22 skipped, 1
deselected` at line 136 and promotes all panels to `Overall: PASS` at line 140.
The summary discloses a deselection count, and source comments explain why, but
the evidence contract requires the exact executed command.

The auditor ran the unfiltered suite: `685 passed, 22 skipped, 1 failed`; a
focused retry failed again with an unclosed UDP socket and still-running `tsp`
subprocess warning. The same test file blob is unchanged from base
(`890498458a6dcfce0e1a3f0b363da3795bc89bd1`). The explicitly deselected panel
then passed `685 passed, 22 skipped, 1 deselected`.

**Required acceptance:** make the generated command header include the exact
`--deselect` node and stop calling the result a full/unfiltered suite. Keep the
known real-boundary failure explicit.

## Remediation disposition

- **CC-WS5-007:** closed at confidence classes 1-3 for construction and
  developer-machine runtime. The actual provider, actual default factory, exact
  by-reference guard wiring, fail-loud DB configuration, alert-kind binding,
  readiness probe behavior, and unchanged downstream builder all pass. Live
  SCM/LocalSystem/session-0 and real service-peer I/O remain correctly unclaimed.
- **CC-WS5-008:** open Major, owner/governance-gated.
- **CC-WS5-014:** open Minor, external PR status drift.
- **CC-WS5-018:** open Minor, committed evidence/status contradiction.
- **CC-WS5-019:** open Minor, committed exact-command/provenance defect.

## Commands and proof re-run

- Live PR identity: `gh pr view 300 --repo scottconverse/civiccast ...` — open
  draft PR, head/base/branch matched the request; body drift recorded above.
- `git status --short --branch`, `git rev-parse HEAD`, `git remote -v`, merge-base,
  and `git worktree list --porcelain` — clean detached audit binding confirmed;
  the coder checkout's unrelated untracked sprint-stop file was not used.
- Complete `01d0e5ee..HEAD` remediation diff, relevant final state, full changed
  status/evidence artifacts, prior verdict, and production call path.
- `git diff --check 0d042dd2..HEAD` — clean.
- `git verify-commit` for all 34 commits in `0d042dd2..HEAD` — 34 good
  signatures, 0 failures; GitHub reports the exact head signature verified.
- Independent committed-evidence reconciliation — all 41 listed blob IDs match
  the exact detached checkout.
- Actual-provider/factory harness — `AUDITOR_FALSIFICATION_PASS`; concrete
  `ProductionDependencies`, `GuardMonitor`, one bound `RuntimeOwnerMutex`, real
  `SupervisorService`, zero instrumented construction-time probe calls, no new
  platform modules during provider construction, SQLite `SELECT 1` true, NATS
  false on closed port, HTTP `status_code=0` on closed port, unset DB error names
  `DATABASE_URL`, and `build_production_service` unchanged.
- `python -m pytest tests/native/test_supervisor_service.py -q` — 36 passed.
- `python -m pytest tests/native -q` — 548 passed in 18.96s.
- `python -m pytest tests/native -q -k "not win"` — 454 passed, 94 deselected.
- `python -m ruff check civiccast/native tests/native` — passed.
- `python -m mypy --strict civiccast/native/supervisor` — success, 13 source
  files.
- `python -m pytest tests/egress -q` — 685 passed, 22 skipped, 1 failed; the
  unchanged real TSDuck relay test failed on resource warnings.
- Focused retry of that TSDuck node — failed again on the same socket/subprocess
  resource warnings.
- Egress with that one node explicitly deselected — 685 passed, 22 skipped, 1
  deselected.
- `python -m pytest tests/policy/test_claims_evidence.py -q` — 213 passed, 10
  failed in 56.59s, caused by the disclosed governed-worker drift.
- Direct external-evidence verifier against current audit-control main — exit 1,
  exactly two governed `worker.py` blob-drift violations.

## CI and artifact status

GitHub run `29936899628` is bound to the audited SHA. At verdict writing, the
NATS real-boundary, Windows-native guard, and live GStreamer jobs had succeeded;
the Linux `Unit tests (3.12)` job was still in progress, so the workflow had no
terminal conclusion and the downstream claims-verifier job was not yet present.
The audit does not promote an in-progress CI run to a pass.

The committed build log's 41 blob rows all bind to this checkout. Its native,
pure-native, ruff, strict-mypy, and explicitly deselected egress results reproduce.
Its displayed egress command and `full-suite` framing do not accurately disclose
the argument that produced the passing panel.

## Skipped, unrun, and field-proof boundaries

- Egress: 22 environment/platform skips and one explicit TSDuck deselection in
  the passing scoped panel; the unfiltered and focused TSDuck runs failed.
- Not run locally: real SCM registration/start; LocalSystem/session-0 boot; real
  PostgreSQL alert write and postmaster containment; real NATS/control-plane
  peers; real SYSTEM-owned named-pipe round trip; elevated registry selector
  mutation; physical DeckLink/GPU/UNC proof; clean machine; soak.
- Provider/factory construction and selected Windows-runtime tests are developer
  machine proof, not Session-0, hardware, pristine-machine, or duration proof.
- No visual/UI surface changed in round 5; the UI/UX lens was not applicable.

## Confidence classes achieved

- **Class 1 — static/source:** exact detached checkout, remediation diff and final
  state, prior-verdict closure, PR/status/evidence reconciliation, claim bindings,
  trust pin, signer registry, source signatures, and evidence blob identities.
- **Class 2 — detached-worktree unit/integration:** actual-provider and default-
  factory falsifications, service/native/egress/claims suites, lint, and strict
  type checking.
- **Class 3 — developer-machine runtime:** real Windows provider/factory
  construction, SQLite engine query, closed-port network failures, real Windows
  native suite, child-process/kernel-object tests, and the attempted TSDuck
  boundary.
- **Classes 4-7:** not achieved in this audit.

## Source-control actions

- No CivicCast implementation, test, documentation, or evidence file was edited.
- No CivicCast commit, push, merge, tag, release, install, SCM service change,
  selector change, interlock change, or cutover was performed.
- This canonical verdict is committed only to audit-control. The PR activity
  comment is telemetry and a pointer, not the authority record.

## Plain-language conclusion

The missing production dependency provider is now real, uses the required
production callables without invoking them during construction, and reaches a
real `SupervisorService` through the actual default factory. CC-WS5-007 closes.

CC-WS5-008 still requires Scott's governance actions, but it is not the sole
remaining issue at this SHA. Correct the stale committed evidence/status text and
exact-command reporting, update the live PR body, preserve the real TSDuck and
field-proof boundaries honestly, then request a fresh exact-SHA audit. No merge
is authorized.
