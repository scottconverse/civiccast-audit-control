# WS5 Supervisor Merged-Tip Audit Verdict - Round 7

- **Verdict:** `BLOCKED`
- **Slice:** `ws5-supervisor` plus the WS4 WSL-detection merge interaction
- **Requested gate:** fresh exact-SHA audit of the owner-integrated `program/native-windows` merge; no verdict inheritance
- **CivicCast repository:** `https://github.com/scottconverse/civiccast`
- **Audited SHA:** `1baf328b47abd857e819f42b946d7d10172130ad`
- **Merge base:** `0d042dd2f6bd3873608c4d257e4e9f1455c910ab`
- **Merge commits:** `d212ee90dadc2ce23ff877d9003b070938d25ba1` (WS4 record descendant) and `1baf328b47abd857e819f42b946d7d10172130ad` (WS5 record descendant)
- **Branch / PR:** historical integration commit on `program/native-windows`; CivicCast PR #300, merged by Scott Converse
- **Live branch observation:** at the audit's live remote fetch, `origin/program/native-windows` resolved to descendant `b56b21e503ff4f5ea2dcd3fa3af71ed1d42b11df`; this verdict remains bound only to `1baf328b...`
- **CI at audited SHA:** no GitHub workflow runs or check-runs exist for the merge SHA
- **Codex auditor thread ID:** `019f8c54-133e-7902-9d58-1a2d6e47a867`
- **Audit date:** 2026-07-22 (America/Denver)
- **Auditor environment:** NVIDEABLACKWELL; Microsoft Windows 11 Pro 10.0.26200 x64; PowerShell 5.1.26100.8875; Python 3.12.13; pytest 9.0.3; ruff 0.15.12; mypy 2.0.0; Git 2.54.0.windows.1; uv 0.11.15; TSDuck 3.44-4676
- **Execution posture:** `sandbox=danger-full-access`; `approval_policy=never`
- **Detached worktree:** `C:\Users\scott\Desktop\CODE\_audit-worktrees\civiccast-ws5-supervisor-1baf328b-r7`
- **Worktree state:** exact detached checkout; tracked tree clean before and after audit execution
- **Activity relay:** CivicCast PR #300 comment `https://github.com/scottconverse/civiccast/pull/300#issuecomment-5052946938`

## Executive result

`BLOCKED` is returned for exactly
`1baf328b47abd857e819f42b946d7d10172130ad`.

The merged source tree is coder-clean under the proof rerun in this round. Both
recorded merges independently recompute to their exact committed tree IDs, with
empty remerge diffs and no conflict-resolution or evil-merge delta. The requested
test panel reproduced `1267 passed, 1 failed, 22 skipped`; the one failure is the
same real-TSDuck resource-warning failure independently reproduced at the
pre-slice base, whose test and implementation blobs are byte-identical at base
and head. Full CivicCast/test ruff and strict supervisor mypy checks pass.

CC-WS5-008 is unchanged and remains the sole blocking gate. The claims suite
returns `213 passed, 10 failed`, and direct external-evidence resolution returns
exactly the two known governed-`worker.py` blob-drift violations. Scott must
decide claim scope/re-attestation, accept a new trust-root pin, and register the
Blackwell auditor key. Those actions necessarily change authoritative bytes and
require a fresh exact-SHA audit.

The sequencing record is explicit: WS5 had only a `BLOCKED` verdict at
`6b3229a93648a9c0c244a7fcaa5512a21019b70f`, never an `AUDIT_PASS`. Scott then
merged its thread-record descendant `e0337cc221dc45babfc094191f121608591309aa`
through PR #300, producing `1baf328b...`, on an explicit owner instruction to
integrate ahead of pass. This audit does not retroactively bless that ordering.
It supplies fresh coverage for the merge SHA and still does not grant a pass.

The public PR body was not reconciled after that owner merge: it still says HEAD
`6b3229a9...` and **No merge**, while GitHub records PR #300 merged at
`1baf328b...`. That is non-blocking provenance drift, recorded below; it does not
alter the code-clean result or replace CC-WS5-008 as the sole blocking gate.

## Severity rollup

- Blocker: 0
- Critical: 0
- Major: 1
- Minor: 1
- Nit: 0

## Findings

### CC-WS5-008 - Major - governed claims and trust root require owner action

`docs/claims/claims.yaml:400` and `:496` bind
`civiccast/egress/gst/worker.py` at
`f00dba1163e24e501184ed517d3228a8293ae70f`; the audited tree contains
`e8a9b621f18d4134ffc93ffe34247dcfdb3ae1d0`. Both
`native-decision-gate` and `session0-service-broadcast` therefore remain invalid.

The independent claims suite returned `213 passed, 10 failed`. Its first-order
failures are exactly those two governed worker-blob violations; the other eight
are cascading registry-clean or isolated-mutation tests that encounter the known
drift. Direct external-evidence mode exited 1 with exactly the same two
violations.

Separately, `docs/claims/trust-root.yaml:37` expects audit-control
`keys/allowed_signers` blob
`0ddf3eb4bbef564f626012a116ccabed0c4f4d65`, while live audit-control main at
audit start contains `29039525d3db5e37b6f8cd77578586212f612f3c` and no
Blackwell `codex-auditor` entry. This verdict is signed with the Blackwell key,
fingerprint `SHA256:CTaM3h09QH5SeJEhGCgq2pM46h7mz3AHcM+g7scP1WA`; principal
matching remains pending Scott's registration action.

**Disposition:** owner/governance-gated. Scott must choose whether to narrow and
re-attest the two claims or unbind the changed worker input, authorize the new
trust-root pin, and register the Blackwell auditor key. A blind blob bump is not
sufficient.

**Blast radius:** claims-verifier CI, external evidence resolution, audit
authority resolution, future auditor signature verification, and the truth of
the native-decision-gate and session-0 claims.

### CC-WS5-020 - Minor - merged PR body still presents the pre-merge state

The live PR #300 body says HEAD is `6b3229a9...` and ends with **No merge**, but
GitHub records the PR merged by Scott Converse at `1baf328b...`. This is a public
provenance contradiction introduced by the owner-directed integration sequence,
not a source-tree or runtime defect.

**Smallest acceptance criterion:** update the PR body to preserve the historical
pre-merge audit state while plainly recording the owner-directed merge commit,
the absence of any pre-merge `AUDIT_PASS`, this round's exact-SHA verdict, and the
still-open CC-WS5-008 boundary.

## Claims checked

### 1. No evil-merge content - proved

`d212ee90...` has parents `0d042dd2...` and `ed34933a...`; the latter is the
audited WS4 SHA `aaf3fe24...` plus only its committed audit-thread record.
`git merge-tree --write-tree 0d042dd2... ed34933a...` produced
`d96100fc596cfb41d365b623d22214830219bcf9`, exactly the recorded merge tree.

`1baf328b...` has parents `d212ee90...` and `e0337cc2...`; the latter is the
WS5 round-6 SHA `6b3229a9...` plus only its committed audit-thread update.
`git merge-tree --write-tree d212ee90... e0337cc2...` produced
`2582adf905b6e434aac91ce1e8fedd2149fcc45f`, exactly the recorded merge tree.

`git show --remerge-diff` was empty for both commits. The six relevant source,
record, and merge commits all passed local `git verify-commit`; GitHub also
reports the two merge signatures valid. No manual resolution or merge-only
content exists.

### 2. Merged-tip test state and base reproduction - proved

At the audited SHA:

`python -m pytest tests/native tests/egress tests/test_app_lifespan_drain_all.py tests/test_app_maintenance_mode.py -q`

returned `1267 passed, 1 failed, 22 skipped in 114.88s`. The sole failure was
`tests/egress/test_ts_relay.py::test_real_tsp_relay_makes_cc_continuous_across_session_reset`,
raising the disclosed pair of unraisable resource warnings: an unclosed UDP
socket and a still-running `tsp` subprocess.

In a separate detached worktree at base `0d042dd2...`, the focused node failed
again in 2.78s with the same two warning classes. Both
`tests/egress/test_ts_relay.py` (blob `890498458a6dcfce0e1a3f0b363da3795bc89bd1`)
and `civiccast/egress/ts_relay.py` (blob
`da05e54774c307936be96c7a3fd4916321a0048d`) are identical at base and audited
head. The failure is pre-existing/environment-dependent and not introduced by
either merge.

### 3. Lint and strict typing - proved

- `python -m ruff check civiccast tests` - passed.
- `python -m mypy --strict civiccast/native/supervisor` - success, 13 source files.

### 4. CC-WS5-008 unchanged - proved

The claims registry, trust-root file, and changed worker blob are byte-identical
between `6b3229a9...` and this merged SHA. The claims suite and direct verifier
reproduced the exact known failure state described in the finding above.

## Commands and artifacts reviewed

- Repo identity: `git status --short --branch`, `git rev-parse HEAD`, remotes,
  merge bases, parent/tree identities, worktree list, and live remote fetch.
- Merge provenance: `git merge-tree --write-tree`, `git show --remerge-diff`,
  parent-relative stats, complete base-to-head changed-file inventory,
  `git diff --check`, and local/GitHub signature checks.
- Runtime: full requested pytest panel, focused base-SHA TSDuck reproduction,
  full `ruff check civiccast tests`, and strict supervisor mypy.
- Governance: `CHARTER.md`, `AUDIT_GATE.md`, `AUDIT_PROTOCOL.md`, the owner WS5
  contract decision, canonical WS4 round-3 and WS5 round-6 verdicts, live
  trust-root/signer bytes, PR #300 state/body, and direct claims resolution.

## CI, skipped checks, and field-proof boundaries

- GitHub exposes zero workflow runs and zero check-runs at the audited merge SHA.
  No CI result is inferred from parent commits.
- The full local panel contains 22 disclosed platform/dependency skips. They are
  recorded as skips, not passes.
- The real-TSDuck node was not skipped; it was run at head and base and failed as
  described. This audit does not relabel that pre-existing failure as clean.
- No fresh Session-0 SCM service run, reboot/resume proof, real PostgreSQL
  containment, live NATS/control-plane peer, SYSTEM-owned pipe round trip,
  pristine Windows machine, physical DeckLink/GPU/UNC path, or soak was run.
- Worktree execution is developer-machine proof, not clean-machine, service
  session, physical-hardware, or duration proof.
- No UI surface changed in the merge interaction; no visual audit was applicable.

## Confidence classes achieved

- **Class 1 - static/source:** exact checkout, complete merge-parent and changed-file
  state, byte-identical merge recomputation, signature checks, governance bytes,
  live PR state, and claim/trust binding.
- **Class 2 - detached-worktree unit/integration:** full requested native/egress/app
  panel, claims suite, full lint, and strict supervisor typing.
- **Class 3 - developer-machine runtime:** real TSDuck process/socket boundary at
  both audited head and pre-slice base.
- **Classes 4-7:** not achieved in this audit.

## Source-control and system actions

- No CivicCast implementation, test, documentation, evidence, branch, PR body,
  merge, tag, release, service, selector, interlock, or cutover state was changed.
- The coder venv was rebound with `pip install -e` to the audited worktree.
- A temporary detached base worktree was used only for the focused reproduction
  and removed after verification.
- This canonical verdict is committed only to audit-control. The PR activity
  comment is telemetry and a pointer, not the authority record.

## Plain-language conclusion

The two integrations did not add hidden merge content or surface a functional
regression under the requested proof. The merge SHA's code is clean, and the one
test failure is independently proven pre-existing at the base. CC-WS5-008 remains
the sole blocking gate and belongs to Scott's governance lane; the stale merged
PR body is a non-blocking provenance cleanup.

This exact SHA therefore remains `BLOCKED`, not `AUDIT_PASS`. WS5 was merged
before any pass on explicit owner instruction, and this verdict does not rewrite
that history. No merge, tag, release, `main` action, installation, or cutover is
authorized by this record. Any owner-remediated descendant requires a fresh
audit and inherits none of this verdict.
