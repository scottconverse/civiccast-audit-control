# Bootstrap Phase 0 Genesis Round-4 Re-audit Verdict

- **Verdict:** `CHANGES_REQUIRED`
- **Slice:** `bootstrap-phase-0`
- **Requested gate:** Bootstrap Phase 0 genesis (`AUDIT_PROTOCOL.md` section 12)
- **CivicCast repository:** `https://github.com/scottconverse/civiccast`
- **Audited SHA:** `48737ad34a21a5072c7649e41b93a08b239b6db8`
- **Merge base:** `f4773a17163b65d7f1b65a413b9bdba976a19ffd`
- **Branch / PR:** `claude/bootstrap-phase-0` / CivicCast PR #283
- **Codex thread ID:** `019f6cfb-27e1-7240-82ba-b81aaca03ca3`
- **Audit date:** 2026-07-16 (America/Denver)
- **Auditor environment:** Windows 10.0.26200.8875 x64; PowerShell; Python 3.14.5; Git 2.54.0.windows.1; `uv` 0.11.15
- **Execution posture:** `sandbox=danger-full-access`; `approval_policy=never` (non-interactive)
- **Detached worktree:** `C:\Users\scott\Desktop\CODE\_audit-worktrees\civiccast-bootstrap-phase-0-48737ad3`
- **Worktree state:** detached at the audited SHA and clean after verification (`git status --short` empty)

## Scope and claims checked

1. **Round-3 authority fix:** confirmed that the working-copy check now compares `git hash-object --path` output with the committed blob identity while structured authority fields are parsed from committed bytes.
2. **Regression suite:** confirmed 17 committed tests, including an asserted-CRLF valid path followed by a substantive tamper rejection.
3. **Independent falsifications:** re-ran the three original forged-authority mutations and independently rebuilt the round-3 fresh-Windows-checkout scenario outside the CivicCast repository.
4. **Cross-file and PR state:** reconciled the changed implementation with the tracked native-Windows operator documentation, source/test module descriptions, and live PR #283 body.

## Severity rollup

- Blocker: 0
- Critical: 0
- Major: 1
- Minor: 0
- Nit: 0

## Finding

### CC-BP0-002 re-opened — Major — The tracked operator documentation and PR front door still describe the rejected raw-byte design

**Evidence:**

- `.agent-runs/native-windows/README.md:29-32` still says the working-copy bytes must equal the committed blob bytes. That is the exact raw-byte behavior rejected at `7590dedc`; the audited implementation intentionally permits CRLF working bytes when their Git-cleaned blob identity equals the LF committed blob.
- `scripts/policy/check_slice_audit_pass.py:27` still characterizes the authority checks as “byte-exact,” while lines 34-39 correctly describe filter-aware blob identity.
- `tests/policy/test_slice_audit_pass_hook.py:10-11` still labels the chain “byte-exact blob,” contradicting its new autocrlf regression.
- The live PR #283 body says `7590dedc` is current, reports 16 tests, and says working bytes must equal committed blob bytes. The live PR head is `48737ad34a21a5072c7649e41b93a08b239b6db8`, the suite has 17 tests, and the implementation is filter-aware.

**Consequence:** the durable operator guide and owner-facing PR description instruct reviewers and future slice operators to reason about the already-falsified design. A legitimate CRLF checkout now passes the code while the documentation says it must fail, and the PR's audit history does not identify the source revision currently under review.

**Blast radius:**

- Adjacent surfaces: the hook module description, test module description, `.agent-runs/native-windows/README.md`, and PR #283's current-design and audit-history sections.
- User-facing/program effect: owner review and later forensic review can approve or troubleshoot the wrong authority contract.
- Migration: none; this is documentation and PR metadata only.
- Tests to update: no behavioral change is required, but the 17-test and autocrlf descriptions must match the existing regression.

**Acceptance criterion:** update the tracked README and both module descriptions to state that the working copy is compared by Git-clean/filter-aware blob identity, with CRLF-only differences accepted and substantive edits rejected. Update PR #283 to name the new SHA/audit history, 17 tests, and the same filter-aware design. Remove every current-surface claim that raw working bytes must equal committed bytes.

## Closed round-3 finding

### CC-BP0-001 — Closed for the filter-aware integrity defect

The implementation and independent runtime reproduction satisfy the round-3 acceptance criterion:

- A hypothetical structured pass record was committed with LF endings in a real-origin audit-control clone.
- A fresh clone under the machine's standard `core.autocrlf=true` setting produced CRLF working bytes and a clean Git status.
- The raw working hash differed from the committed blob, while `git hash-object --path` equaled the committed blob.
- The documentation-exact TaskCompleted replay exited 0.
- A substantive post-checkout edit changed the filtered blob identity and the same hook replay exited 2 with the expected mismatch reason.

Fixture identities:

- local fixture authority commit: `d69649d6f14409da3b586e12a5c80893088be53c`
- committed/filtered blob: `8f587d0b0276608f504d6d712577903d31d104e0`
- clean CRLF raw working hash: `3c60bdaaced42fe5d727a4128578f3baf55a53b9`
- tampered filtered blob: `1b22182aa772a351d6594224b61368e31f7880bd`

## Commands and negative controls re-run

- `git diff --check 7590dedca02c6d483f1edb42c577cfb260c3d192..48737ad34a21a5072c7649e41b93a08b239b6db8` -> exit 0.
- Scoped fix diff -> two changed files, 40 insertions and 7 deletions.
- `python -m pytest tests/policy/test_slice_audit_pass_hook.py -q` -> `17 passed in 5.03s`, exit 0.
- `uvx ruff check scripts/policy/check_slice_audit_pass.py tests/policy/test_slice_audit_pass_hook.py` -> all checks passed, exit 0.
- Independent `bootstrap_phase_0_falsifications.py` -> `3 passed in 0.96s`, exit 0; non-pass record with pass-token prose, forged repository, and moving governance ref all block with exit 2.
- Independent clean `core.autocrlf=true` checkout -> CRLF premise true, raw hash unequal, filtered hash equal, clean Git status, hook exit 0.
- Same checkout after substantive edit -> filtered hash unequal, hook exit 2.
- Documentation-exact slice payload without cache -> exit 2.
- Non-object JSON payload `[]` -> exit 0.
- Invalid slice ID -> exit 2.

## Five-lens result

- **Engineering:** pass; the filter-aware comparison is correctly scoped and committed authority parsing remains checkout-independent.
- **UX:** no product UI changed; hook failure messages remain ASCII-safe and actionable.
- **Documentation:** one Major finding; tracked and PR descriptions contradict the implemented authority model.
- **Tests:** pass; the new regression asserts the CRLF premise and mutation-tests the same verifier with a substantive edit.
- **QA/runtime:** pass at developer-machine integration level for both the clean and tampered fresh-checkout paths.

## Confidence classes and boundaries

- **Class 1 (static/source):** exact detached SHA, complete two-file round-4 diff, final related docs, governance contracts, and live PR metadata.
- **Class 2 (unit/integration):** committed 17-test suite and independent forged-authority harness.
- **Class 3 (developer-machine runtime):** independent real Git clone/reclone under the installed Windows `core.autocrlf=true` configuration, with clean and tampered hook replays.
- **Not established:** class 4 session-0 service, class 5 physical hardware/live peer, class 6 pristine Windows machine, or class 7 soak.

## Known gaps and skipped checks

- The hook was not live-fired through the real Claude Code TaskCompleted harness; this remains an accepted out-of-gate gap.
- The `codex-auditor` MCP loop was not exercised; this request arrived through `codex exec` before MCP approval.
- Media runtime evidence was not re-executed because this round's two-file diff only changes hook integrity logic and its test. Prior-SHA runtime proof is not represented as a new-SHA rerun.
- Source switching, recovery, multichannel behavior, session-0, packaging, licensing closure, and soak remain outside this Bootstrap Phase 0 gate.
- No merge, tag, release, cutover, or CivicCast implementation edit was performed.

## Final rationale

`CHANGES_REQUIRED` is required because the source and PR documentation still assert the raw-byte authority behavior that this SHA correctly replaces. The round-3 Critical implementation defect is closed and independently mutation-tested, but Bootstrap Phase 0's durable operator and owner-review surfaces must describe the actual filter-aware contract before advancement. This verdict applies only to `48737ad34a21a5072c7649e41b93a08b239b6db8`. Scott Converse remains the only merge and advancement authority; coder/auditor disagreement goes to him.
