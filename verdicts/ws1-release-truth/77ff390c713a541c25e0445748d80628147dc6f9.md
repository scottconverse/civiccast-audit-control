# WS1 Release-Truth Round-2 Re-audit Verdict

- **Verdict:** `CHANGES_REQUIRED`
- **Slice:** `ws1-release-truth`
- **Requested gate:** Charter section 8 row 1, scoped to manifest, read-only checker, and external observer
- **CivicCast repository:** `https://github.com/scottconverse/civiccast`
- **Audited SHA:** `77ff390c713a541c25e0445748d80628147dc6f9`
- **Merge base:** `44463a197f326b7e607c31c56f28047ff1b00fb0`
- **Prior audited SHA:** `348e5b1ccce74fd13f03f76179faf8e8b5745a9f`
- **Branch / PR:** `claude/ws1-release-truth` / CivicCast PR #287, targeting `program/native-windows`
- **Codex thread ID:** `019f6cfb-27e1-7240-82ba-b81aaca03ca3`
- **Audit date:** 2026-07-16 (America/Denver)
- **Auditor environment:** Windows 10.0.26200 x64; PowerShell; Python 3.14.5; Git 2.54.0.windows.1; `uv` 0.11.15
- **Execution posture:** `sandbox=danger-full-access`; `approval_policy=never` (non-interactive)
- **Detached worktree:** `C:\Users\scott\Desktop\CODE\_audit-worktrees\civiccast-ws1-release-truth-77ff390c`
- **Worktree state:** detached at the audited SHA and clean after verification
- **Activity relay:** one round-specific updatable comment on CivicCast PR #287: <https://github.com/scottconverse/civiccast/pull/287#issuecomment-4997530353>
- **Heartbeat result:** required start, phase-transition, and finish milestones were relayed; no relay failure affected the audit

## Scope and gate boundary

The re-audit covered the complete `348e5b1c..77ff390c` fix diff, all five changed files, live PR #287, current audit-control governance at observer commit `5c0d56400d129bd68d7a0fd021b7bdcd21e26b7f`, observer run 29542897314, audit-control issue #3, current GitHub releases, and exact live `main` README at `61e0449ecd5565889a711da5c6f9e1e1eb51af58`.

The user's narrowed gate asks whether the tooling handles the current rc15 rollout truthfully, not whether the external rc-line state is clean. The single live-main drift is therefore accepted as honest-red external state, not an implementation finding. Generated README tooling, assets/hashes/signing truth, release-event triggers, and full charter-row closure remain outside this scoped verdict.

## Severity rollup

- Blocker: 0
- Critical: 0
- Major: 2
- Minor: 2
- Nit: 0

## Findings

### CC-WS1-004 — Major — The committed evidence hashes do not match the audited files

- **Surface:** `.agent-runs/native-windows/ws1-release-truth/evidence/build-verification.log:11-13`; `.agent-runs/native-windows/ws1-release-truth/evidence/run_evidence.py:27-32`
- **Evidence:** independent SHA-256 recomputation at detached `77ff390c` produced:
  - checker actual `8a2e1d5720bc19f53cfb43a1795f8da226bbeba6e388a0862487abe359112fc1`; log records `061dc25226d280f5db21ce35f18244d29569ea7b3bc0722f17a695ee4b29479a`;
  - manifest actual `c83bb1bc98714dbd7253cb3fc8b56e6e6ab94ec043e249e02c4ba339869bfb01`; log records `e6d20fa46738b5e3327d12f437d607516b2309ff1644cd8bd4999f2626bc7940`;
  - tests match at `5243bac38857710d2862fbb5834a61f3a46ef62a9cb3a4432d6c42a97a6a9b5f`.
- **Consequence:** the central acceptance test for CC-WS1-004 fails. The committed PASS transcript was generated before the final checker and manifest bytes and is not proof for the audited implementation. The runner also omits the README input hash and the environment/tool-version record required by `CLAUDE.md:100-102` and the prior acceptance criterion.
- **Acceptance criterion:** freeze the implementation, update the runner to hash every tested input including the relevant README and record the execution environment/tool versions, regenerate the log last, commit it, and require every recorded digest to match independent recomputation at the new audited SHA.

### CC-WS1-002 — Minor — Input-boundary hardening still has uncaught valid-YAML shapes

- **Surface:** `scripts/policy/check_release_truth.py:96-103`, `110-111`, `129`
- **Evidence:** the six original independent controls now pass, including the three requested cannot-check cases. Three additional syntactically valid manifests still produce tracebacks and process exit 1 rather than concise `ERROR:` plus exit 2:
  1. `historical_unlisted_patterns: [42]` -> uncaught `TypeError` from `re.compile`;
  2. list-valued entry tag -> uncaught unhashable `TypeError` in `set(tags)`;
  3. current entry with no `tag` -> uncaught `KeyError`.
- **Consequence:** the checker remains fail-red, not silent-green, and the observer would still alarm. However, exit 1 is reserved for evaluated drift, so callers still cannot reliably distinguish these cannot-check states from real drift.
- **Acceptance criterion:** validate pattern element types and required scalar entry fields before compiling, hashing, or indexing them; commit the three controls and require exit 2, an `ERROR:` diagnostic, and no traceback.

### CC-WS1-005 — Major — PR #287 still describes the prior implementation and observer

- **Surface:** live CivicCast PR #287 body
- **Evidence:** at audited head `77ff390c`, the PR body still reports 13 tests instead of 19, identifies observer commit `dc03acd` as pinned to this slice even though it pins `348e5b1c`, describes the pre-rc15 manifest without the staging entry, and does not name the current observer commit `5c0d564` or run 29542897314.
- **Consequence:** the public review surface sends the owner and future reviewers to stale proof and materially misstates the test count and release-state model. Repo audit policy treats observable-behavior documentation drift as an in-scope defect.
- **Acceptance criterion:** reconcile the PR body with the current head: 19 tests, rc15 staging semantics, authenticated observer at `5c0d564` pinned to the current slice, the evaluated exit-1 run, the honest-red live-main drift, and current evidence status.

### CC-WS1-006 — Minor — The new evidence runner is outside the claimed clean lint set

- **Surface:** `.agent-runs/native-windows/ws1-release-truth/evidence/run_evidence.py:49`, `:65`
- **Evidence:** the claimed checker/test lint command passes. Extending the same repo ruff configuration to the new Python runner reports `S603` at the generic `subprocess.run` call and `UP017` for `datetime.timezone.utc`.
- **Consequence:** the executable artifact added specifically to close an audit finding is not clean under the repository's lint policy, while the handoff says “ruff clean.”
- **Acceptance criterion:** include the evidence runner in the documented lint command and make that command pass under the repository configuration.

## Closed prior findings

- **CC-WS1-001:** closed. Every supplied `superseded_by` is checked, both `withdrawn` and `superseded` require a successor, two regressions pass, and the prior dangling-successor mutation now goes red.
- **CC-WS1-003:** closed. `fetch_releases` accepts `GITHUB_TOKEN`/`GH_TOKEN`, the audit-control workflow supplies `github.token`, and run 29542897314 performed an authenticated evaluation instead of hitting the unauthenticated limit. It correctly returned checker exit 1 for exactly one live-main README drift and updated issue #3.

## Commands and proofs re-run

- `git diff --check 348e5b1c..77ff390c` and `git diff --check 44463a19..77ff390c` -> exit 0.
- `python -m pytest tests/policy/test_release_truth.py -q` -> `19 passed in 2.19s`, exit 0.
- `uv run ruff check scripts/policy/check_release_truth.py tests/policy/test_release_truth.py` -> all checks passed, exit 0.
- Ruff with the newly added evidence runner included -> two findings (`S603`, `UP017`), nonzero.
- Prior independent six-control harness -> `6 passed`, exit 0.
- Additional three-shape harness -> `3 failed`; all observed exit 1 tracebacks where controlled exit 2 was required.
- Independent SHA-256 recomputation -> checker and manifest mismatch; test digest matches.
- Authenticated checker against audited checkout README -> `release-truth: PASS`, exit 0.
- Authenticated checker against exact live-main README at `61e0449e` -> exactly `DRIFT: README.md never mentions the current release v1.0.0-rc14`, exit 1.
- Observer run 29542897314 -> workflow SHA `5c0d564`, checker pin `77ff390c`, `GITHUB_TOKEN` present, exactly one rc14 README drift, checker exit 1, issue #3 updated.
- Static authority trace -> observer retains only audit-control `contents: read` and `issues: write`; no CivicCast release, tag, merge, or status-check mutation path was added.

## Audit-lite dimensions

- **Correctness and security:** release/successor checks and authenticated observer behavior work; evidence binding and malformed-shape classification remain incorrect.
- **UX:** no product UI changed. Operator errors are improved for the named cases but still expose tracebacks for adjacent valid-YAML shapes.
- **Documentation:** manifest staging prose is present; live PR body is materially stale.
- **Tests:** 19 committed tests pass and all six prior mutations close. Three adjacent input-shape controls remain red.
- **Runtime:** local authenticated live evaluation and the real hosted observer path were executed. The checker truthfully distinguishes the audited README PASS from exact live-main drift.
- **Escalation:** no audit-team escalation is warranted; findings remain confined to this slice.

## Confidence classes and boundaries

- **Class 1:** exact detached SHA, full fix diff, final code/tests/manifest/evidence, live PR metadata, governance workflow, issue state, and read-only authority trace.
- **Class 2:** current-SHA 19-test suite, six prior mutations, three additional shape attacks, lint, and digest recomputation.
- **Class 3:** authenticated developer-machine live GitHub check against both audited and exact live-main README bytes.
- **External observer:** hosted authenticated evaluation and issue relay proved at run 29542897314.
- **Honest-red external state:** main currently omits rc14 while the manifest holds rc15 as staging. This is one correctly detected drift, not a tooling defect and not release authority.
- **Not established:** generated README tooling, assets/hashes/signing truth, release-event triggers, or full charter-row closure.
- No merge, tag, release, cutover, CivicCast branch edit, or implementation fix was performed.

## Final rationale

`CHANGES_REQUIRED` is bound only to `77ff390c713a541c25e0445748d80628147dc6f9`. The core successor validation and authenticated observer fixes work, and the current external drift is reported correctly. Advancement is nevertheless unsupported because the claimed commit-binding proof fails direct digest recomputation and the PR's public audit surface remains materially stale. The remaining input-shape and lint issues are lower severity but should be closed in the same narrow follow-up.

This verdict does not merge or advance the slice. Scott Converse remains the only merge and advancement authority; coder/auditor disagreement goes to him.
