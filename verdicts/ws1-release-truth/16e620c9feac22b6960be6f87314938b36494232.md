# WS1 Release-Truth Round-7 Re-audit Verdict

- **Verdict:** `CHANGES_REQUIRED`
- **Slice:** `ws1-release-truth`
- **Requested gate:** Charter section 8 row 1, scoped to manifest, read-only checker, evidence, and external observer
- **CivicCast repository:** `https://github.com/scottconverse/civiccast`
- **Audited SHA:** `16e620c9feac22b6960be6f87314938b36494232`
- **Merge base:** `44463a197f326b7e607c31c56f28047ff1b00fb0`
- **Prior audited SHA:** `f09670ace8b7154fbd2a2f87e93999fbed60c504`
- **Branch / PR:** `claude/ws1-release-truth` / CivicCast PR #287, targeting `program/native-windows`
- **Codex thread ID:** `019f6cfb-27e1-7240-82ba-b81aaca03ca3`
- **Audit date:** 2026-07-16 (America/Denver)
- **Auditor environment:** Microsoft Windows NT 10.0.26200.0 x64; PowerShell; Python 3.14.5; Git 2.54.0.windows.1; `uv` 0.11.15
- **Execution posture:** `sandbox=danger-full-access`; `approval_policy=never` (non-interactive)
- **Detached source worktree:** `C:\Users\scott\Desktop\CODE\_audit-worktrees\civiccast-ws1-release-truth-16e620c9-r7`
- **Detached base worktree:** `C:\Users\scott\Desktop\CODE\_audit-worktrees\civiccast-ws1-base-44463a19-r7`
- **Worktree state:** both fresh, exact, detached, and clean before and after verification
- **Activity relay:** <https://github.com/scottconverse/civiccast/pull/287#issuecomment-4997890035>
- **Heartbeat result:** start, all applicable phase transitions, long-command notice, long-command result, and finish were relayed; no relay failure occurred

## Scope and result

The audit covered the complete `f09670ac..16e620c9` diff, the final evidence runner/log/control, live PR #287, audit-control observer pin at `8d01098af2b6e82e8511cf91b2c130b904a4d603`, the slice and hook regressions, authenticated live-release behavior, and the disclosed full-policy failures compared with exact integration base `44463a19`.

The conditional disk-byte wording is now accurate. The new `tests/policy/test_hash_semantics_control.py` is self-contained, asserts an actual CRLF premise, proves the two filtered forms equal the committed blob and `--no-filters` differs, passes independently, and goes red when the raw expectation is inverted.

CC-WS1-007 is nevertheless only partially closed. The round-6 acceptance criterion required the control in the evidence runner’s **hashed/rerun proof path**. `run_evidence.py` neither lists the new test under `HASHED_FILES` nor invokes it in its pytest command. The regenerated log therefore binds only the prior four files and records only the original 22 tests. The new docstring’s statement that semantics are locked behind the evidence runner’s blob-identity binding is not true of the implemented runner.

## Severity rollup

- Blocker: 0
- Critical: 0
- Major: 1
- Minor: 0
- Nit: 0

## Finding

### CC-WS1-007 - Major - The committed control is still outside the evidence runner’s hashed and executed proof set

- **Dimension:** correctness / tests / documentation / evidence integrity
- **Surface:** `.agent-runs/native-windows/ws1-release-truth/evidence/run_evidence.py:40-46,108-113`; `.agent-runs/native-windows/ws1-release-truth/evidence/build-verification.log:18-31`; live PR #287 evidence/test paragraphs
- **Evidence:** the new control has committed blob `c1341d404d6adff1d4a8eed1ae5c7ebcd25270d1`, but that path is absent from `HASHED_FILES` and from every blob line in the log. The runner still executes only `python -m pytest tests/policy/test_release_truth.py -q`, and the regenerated log records `22 passed`; it does not execute `test_hash_semantics_control.py`. The PR body likewise advertises 22 tests and four blob IDs even while claiming the control is delivered.
- **Why it matters:** the control works when invoked directly, but the committed evidence path can still report `Overall: PASS` without hashing or running it. A future regression in the control or the semantics it protects would be invisible to the very transcript claimed to bind and rerun that proof. This is the remaining proof gap from CC-WS1-007, not a functional checker defect.
- **Acceptance criterion:** add `tests/policy/test_hash_semantics_control.py` to `HASHED_FILES`; change the evidence pytest command to run both policy test files (23 current tests); update the log/header and PR body to report five bound inputs and the 22+1/23-test execution accurately; regenerate `build-verification.log` last. Recompute all five blob IDs against the new SHA. The already-working mutation control should remain red under the inverted expectation.

## Closed portions of CC-WS1-007

- Conditional checkout-filter wording is accurate and contains no “never matches” absolute.
- The committed control’s Git setup is isolated from global `core.autocrlf`, asserts CRLF working bytes, and proves the required hash relationships.
- Direct positive run passes; the in-memory mutation replacing `no_filters != committed` with `no_filters == committed` exits 1 with the expected assertion.
- PR history correctly retracts the earlier false causality and binds current head and observer pin to `16e620c9`.

## Proofs re-run

- Live repository/PR binding -> exact head `16e620c9feac22b6960be6f87314938b36494232`, open PR, base `program/native-windows`, exact observer pin, and explicit no-`main`/no-release-authority boundary.
- Full scoped diff -> one commit; three changed files; 67 insertions and 8 deletions; `git diff --check` clean.
- `python -m pytest tests/policy/test_hash_semantics_control.py -q` -> `1 passed in 0.20s`, exit 0.
- In-memory mutation of the raw expectation -> exit 1 with `AssertionError: MUTATION: incorrectly expect raw bytes to equal filtered blob`.
- `python -m pytest tests/policy/test_release_truth.py tests/policy/test_hash_semantics_control.py -q` -> `23 passed in 3.74s`, exit 0.
- `python -m pytest tests/policy/test_slice_audit_pass_hook.py -q` -> `17 passed in 5.52s`, exit 0.
- Four-file ruff command including both policy test files and the runner -> all checks passed, exit 0.
- Existing evidence identities -> checker `9c9cbb7ed334e394ff676eb833058c98e01923b8`, manifest `5ffae14585c7b067620acad0ada6db5b5063e744`, release-truth tests `92e95121949f6a6a3814ac8a6d1f5a6f2f716edc`, and README `dbd62da5180732df7e1f9ece56fc6b6964296f2e` all equal their logged and committed blobs.
- Control binding check -> committed blob `c1341d404d6adff1d4a8eed1ae5c7ebcd25270d1`; path absent from the evidence log and runner’s `HASHED_FILES`.
- Authenticated checker against audited checkout README -> `release-truth: PASS`, exit 0.
- Authenticated checker against exact live CivicCast `main` README at `61e0449ecd5565889a711da5c6f9e1e1eb51af58` -> exactly `DRIFT: README.md never mentions the current release v1.0.0-rc14`, exit 1.
- Observer workflow at audit-control `8d01098` -> exact `16e620c9` pin, `contents: read`, `issues: write`, and no CivicCast release authority.

## Full-policy disclosure comparison

- Audited head: `python -m pytest tests/policy -q` -> `5 failed, 284 passed, 1 skipped in 22.43s`.
- Exact integration base `44463a19`: same command -> `5 failed, 261 passed, 1 skipped in 19.59s`.
- Both runs fail the identical node IDs and conditions:
  - three tests in `test_held_rc10_docs.py`;
  - `test_v17_adoption_gate_passes_current_repo`; and
  - `test_windows_install_doc_holds_unproven_candidate`.
- These failures are exact-base pre-existing rc-line documentation-policy drift. WS1 does not change their tests or asserted public-doc surfaces. They remain honest-red external/base state and do not create an additional WS1 finding.

## Audit-lite dimensions

- **Correctness and security:** release checker and read-only observer behavior pass; no security issue found.
- **UX:** no product UI changed; current PR history and authority language are accurate.
- **Documentation:** conditional semantics are correct, but the evidence-binding and test-count claims overstate what the runner performs.
- **Tests:** the control itself is strong and mutation-sensitive; evidence-runner integration is missing. Broader failures are base-identical.
- **Runtime:** authenticated audited-checkout evaluation passes and live `main` produces exactly the known one-drift result.
- **Escalation:** no full audit-team escalation is warranted; the sole finding is a bounded evidence-runner integration omission.

## Confidence and boundaries

- **Class 1:** exact fresh source/base worktrees, complete diff/final evidence, live PR body, observer pin, issue state, and authority trace.
- **Class 2:** control positive/mutation runs, 23-test slice suite, 17-test hook suite, lint, four bound blobs, and full policy head/base comparison.
- **Class 3:** authenticated developer-machine evaluation against audited and exact live-main README inputs.
- The exact observer pin is statically verified; hosted authentication remains demonstrated by the prior observer run.
- Audit-control issue #3 remains the known honest-red rc14/rc15 drift, not a checker defect or release authority.
- No service-session, hardware, clean-machine, or soak claim is part of this gate.
- No merge, tag, release, cutover, CivicCast branch edit, implementation edit, or PR-body edit was performed.

## Final rationale

`CHANGES_REQUIRED` is bound only to `16e620c9feac22b6960be6f87314938b36494232`. The functional machinery, corrected prose, committed control, mutation behavior, closed-set regression, and live behavior are sound. Advancement remains unsupported because the canonical evidence generator can still pass without binding or executing the new control that was expressly required to prevent recurrence.

Scott Converse remains owner and tie-breaker. This verdict authorizes no merge. A future exact-SHA `AUDIT_PASS` can authorize merge only into `program/native-windows`, never CivicCast `main`, and never a tag or release.
